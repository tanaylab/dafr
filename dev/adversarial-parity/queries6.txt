@ gene : missing_prop % Fraction type int32 >> Var || '0'
@ cell : ?
@ type : missing_prop || 0b10
@ cell @ cell [ all_nan = 65535 | ! f32_score <= 0b10 ] :: distance
. intver || 1e10 Char % Clamp low nan
@ type [ missing_prop ~ 32767 ] : missing_prop
@ empty_axis : ?
@ cell @ gene :: sparse_umis >> VarN
. flag || 127
@ cell [ all_nan !~ 0b10 & all_nan != -32768 ] : ties_int / with_nan >> Var type Char
@ cell @ cell [ u32_count < -1.0 ] :: distance
@ type [ color >= 0.5 ] : color || 2147483648 UInt64
@ type : missing_prop
@ cell @ cell :: distance >- Var
@ gene [ is_lateral <= 4294967296 ] : marker % Clamp low e high 65535 >> Sum || Inf
@ empty_axis
@ gene [ marker != 2147483648 ^ is_lateral !~ 0.0 ] : marker / marker =@
@ cell [ subtype > a b & label >= V ^ f32_score != -Inf ] : age % Convert type UInt64 >> Quantile
@ cell : score
@ type [ color ~ 2147483648 | color < 1e10 ] : missing_prop
@ cell @ gene :: UMIs >> Std type Int32
@ type [ missing_prop > 1e308 ] : color >> Median type String
@ cell @ gene :: frac
. ?
@ cell @ gene :: sparse_umis >| Median type Bool || 128
@ cell @ gene :: frac % Significant low 0 >| Mode
@ cell [ age <= 128 ^ ! all_neg ~ inf ^ infs < 1.5 ] : age % Round digits 3
. intver || 1.5
@ cell [ ties_int > 1 ] : i8_age || 0
. missing_scalar || pi
@ batch [ missing_prop = Infinity | missing_prop != 100 ] : donor >> VarN type Int8 eps 0.0
@ cell @ gene [ u32_count < 4294967295 & ! neg_age <= 32767 ] :: sparse_umis % Clamp low 127 high .5
. title
@ cell [ ! label >= Z ] : label / is_doublet =@ || e
@ cell : type =@ : color % Clamp high NaN
@ cell : ties_int / infs =@ || 1_000
@ cell : is_low >> Std type Bool
. intver || pi
. missing_scalar || -1.5 String
@ cell [ ! all_neg <= 1_000 | ! batch !~ Z & all_nan ~ 1_000 ] : neg_age % Abs / is_doublet >> Quantile
@ gene [ marker ~ -1 ] : marker % Abs type int32 >> Min
@ type : color / color =@ >> Min
@ cell [ missing_prop < -1.5 & age <= 0.0 ] : i8_age / missing_prop =@
@ batch [ donor != 1e-10 ] : missing_prop >> GeoMean eps Infinity
@ cell [ with_nan !~ 1e308 ] : all_zero % Abs type Bool / age =@
@ cell @ gene [ ties_int != 1.0 ^ u32_count > 127 | batch <= '' ] :: UMIs % Abs type int8
@ batch [ donor !~ 1e-10 ] : donor % Log base 1_000 eps 4294967295 / donor >> Median
@ cell @ gene :: sparse_umis >- Count type Float32
@ cell : infs >> GeoMean eps e
@ cell [ infs > -128 | ! all_nan <= 4294967296 & ! all_nan = 0.0 ] : age % Abs type UInt8
@ cell @ gene [ missing_prop ~ Inf & i8_age <= 1e10 ] :: sparse_umis
. flag
. version || 1e-10
. version
. missing_scalar
@ batch : missing_prop % Abs type Int16 >> Var
@ cell @ cell :: distance % Significant high Infinity low Inf >> Count
@ batch [ ! donor ~ 32767 ] : missing_prop % Clamp low 4294967295 / donor >> Sum type UInt16
. intver
@ batch : missing_prop
@ cell [ label !~ '' & missing_prop > 256 ] : u32_count % Fraction type Float32
@ cell [ u32_count != -128 & is_doublet <= -1 ] : all_neg % Fraction || 10
@ batch : donor % Fraction type float32 / donor >> Min
@ cell [ ! score !~ inf ] : all_nan % Convert type UInt64 >> Sum
@ cell [ ! i8_age < 32767 & score < -1.0 ^ is_low ~ 1e308 ] : all_nan % Clamp >> Mean type UInt64
@ type [ color !~ inf & color != 1 ] : missing_prop >> Mode
@ cell @ cell [ score > -Inf ] :: distance >> StdN
@ cell @ gene :: sparse_umis % Log base 127
@ gene : is_lateral % Clamp low 256 high 1.5
@ batch [ donor >= -Inf ] : missing_prop % Convert type Int16 >> Sum
. intver || 0x10
@ cell @ cell :: distance
@ type [ color != 1e10 ] : missing_prop % Log / color =@ >> Min || 4294967296
@ ?
@ cell [ missing_prop != 4294967296 ] : ties_int % Log base 0 eps 2147483648 >> Var type Int64 || -Inf
@ gene @ cell :: ?
@ cell @ gene [ ! label > V | i16_age = 65535 ] :: sparse_umis >> Min
@ cell : infs % Log base -1 eps Infinity || 4294967295 String
@ cell @ cell :: distance % Clamp low 0 high 1.0 >- GeoMean
@ cell @ gene :: frac >- Var
@ cell @ gene [ subtype != Z ^ batch <= W ] :: UMIs >> VarN eps 0 || 'true'
. version || 32767
@ gene [ ! missing_prop >= 2147483647 ^ ! marker <= 1e10 ] : missing_prop / marker =@
@ cell : i16_age || 'pi'
@ batch : missing_prop % Log base 1e-10 eps 2147483647 / donor >> Max
@ cell @ gene :: ?
@ batch : donor % Log base 100 || ''
@ cell : label % Log base 1 / infs >> Min
@ cell @ gene [ subtype >= '' ^ ! infs !~ 127 | neg_age ~ -1.0 ] :: frac % Round digits 2 type float32 >> Median type UInt8
. intver || 'true'
@ cell : label % Significant low 1e308
@ cell @ gene :: frac % Log base e eps 1 >> Min
@ batch : missing_prop >> Var type float32
@ cell @ gene [ ! all_zero ~ -1.0 | ! missing_prop != NaN ] :: frac >| GeoMean
@ cell @ gene :: sparse_umis % Convert type Int8 >> Mode
@ cell [ missing_prop > 1e10 & ! ties_int = 1_000 ] : ties_int
. version || 1.0 Symbol % Convert type NotAType
@ batch : missing_prop % Log base -1 eps 0b10
@ type [ missing_prop = 65535 ] : color % Log base 0b10 eps -1.5 >> Max
@ cell : u32_count / all_nan =@
@ cell @ gene :: frac % Round digits 1 type String
@ cell [ ! i16_age ~ nan ] : batch / u32_count
@ type : color >> Min || 65535
@ gene [ is_lateral = 1e308 | marker != 65535 | missing_prop >= -1 ] : missing_prop % Log eps Inf % Clamp low .5 high 4294967295 / marker || 32767 Int64
@ cell @ gene [ all_nan >= -Inf ] :: UMIs
@ cell @ gene [ i8_age >= Infinity & is_low = 256 ] :: sparse_umis >- Sum
@ cell [ is_low != inf ] : all_neg % Fraction
@ cell @ gene :: frac || 32767
@ cell @ cell :: distance || -32768
@ cell @ gene [ ! all_zero ~ Infinity ] :: frac % Significant high 5. low 1.5 >> Count
@ type : missing_prop % Clamp low -1 / color >> Sum type Int32
@ cell : is_doublet % Log base 100 || 2147483647
@ cell @ gene :: UMIs >| VarN eps -1
@ gene : marker % Fraction type Symbol >> Mode
@ cell @ gene :: sparse_umis >| Sum type Char
@ batch : missing_prop % Significant high -1.5 low 1.5 >> Mode || 1e10
@ cell [ age >= -1 ] : all_zero % Significant / type
@ cell : all_zero % Convert type NotAType >> StdN eps 1
. version || 100 bool
@ cell [ i16_age !~ 4294967296 | i16_age >= 1.0 ] : infs % Convert type Int32
@ gene : marker % Abs type NotAType
@ cell @ gene [ all_zero >= 1_000 ] :: UMIs % Fraction type Symbol >- Mode
@ cell @ cell :: distance % Significant high 256 low -1 >- Var
@ cell [ i8_age = 0b10 & subtype < V ] : all_neg / label
@ cell @ gene :: sparse_umis % Convert type Int8 >> GeoMean || -1.0
@ cell @ gene [ missing_prop <= 1_000 ] :: UMIs % Significant high 5. >> Mode
@ cell : ties_int % Clamp low -128 high Inf >> Std type Float32
@ cell : infs >> GeoMean eps 1.0
@ cell [ i8_age != -1.5 ] : all_zero / type =@
@ type [ missing_prop <= 1 & color != 2147483647 | ! color ~ 1e10 ] : missing_prop
@ cell @ gene [ type <= '' ^ infs >= 0.0 & batch < W ] :: sparse_umis % Fraction >| StdN
@ cell [ i16_age != 1 ] : is_doublet / is_low >> Std type float64
@ cell : all_neg / subtype >> Median type Int64
@ cell @ gene :: frac >- Mode || 65535
@ gene [ missing_prop > -1 ] : is_lateral
@ type : missing_prop % Convert / color
@ type : color % Convert / color =@ >> StdN eps 1e-10
. missing_scalar || 127
@ cell @ gene :: UMIs % Convert type bool
@ cell @ cell [ is_doublet < .5 ] :: distance
@ gene : marker % Clamp low -32768 >> Median
@ cell @ gene :: sparse_umis >> Quantile p 0.5
@ cell [ age < 1.0 ^ with_nan >= 0.5 | age = inf ] : with_nan % Fraction
@ cell @ gene :: sparse_umis
@ batch : missing_prop / donor =@ >> Sum type Int16
@ type : missing_prop % Fraction type NotAType / color =@ >> Var type Char || 4294967296 Bool
@ cell @ gene :: sparse_umis % Log >> Mode
@ gene [ missing_prop > 1e-10 ] : is_lateral >> Max
@ cell @ cell [ is_doublet > 1.5 ^ i16_age < nan ^ ties_int > 1 ] :: distance % Round digits 3 type int32 >- Mode
@ cell : all_neg % Fraction
@ cell : label % Significant low 65535
. title % Abs type NotAType
@ type : missing_prop / color >> Std type UInt64
@ cell @ cell :: distance >- Max || 1e308 Bool
@ batch : donor % Abs type float32 / donor
@ type : color % Round digits 0
@ cell [ ! i8_age <= 0x10 & ! age = 0x10 ] : label / neg_age >> Max
@ gene : missing_prop % Abs type Float64 % Log eps e >> StdN
@ type [ missing_prop > 256 ] : color / color || -1
@ gene [ marker != -128 ] : missing_prop % Convert / marker
@ gene [ is_lateral <= 4294967296 ] : marker
@ type : color % Clamp low 2147483647 >> Min
@ cell [ missing_prop = NaN & type < U ] : subtype >> Var type Symbol || NaN bool
@ cell @ gene :: sparse_umis >| Quantile p 0.25
. missing_scalar || pi float32 % Fraction type UInt16
@ cell @ gene :: sparse_umis >| Count type int8
@ gene : marker % Significant high .5 low 0.0 % Log
@ cell [ all_nan !~ -32768 & neg_age < -1.0 & ! label !~ Z ] : age / score =@ >> Mode || 2147483647 UInt8
@ cell [ subtype < Z & ! with_nan > -1.5 | type != U ] : f32_score % Fraction type NotAType >> Mean type Int32
@ gene [ is_lateral = 10 | is_lateral <= nan ] : is_lateral % Significant high -Inf low -1.5 % Round digits 0 type UInt8 >> Sum || 4294967296
@ cell [ u32_count != 4294967296 ] : type >> Mean
@ type [ color >= 127 ] : color % Log base Inf eps NaN / color =@ || nan
@ cell [ score > 5. ] : neg_age % Convert >> Mode || 0.5
@ cell : batch >> Median
@ cell @ cell :: distance >> Median || 1
@ type : color >> Var
@ cell : subtype / i16_age >> Count
. intver || 127
@ type [ missing_prop !~ NaN ] : color % Fraction type Int32
@ cell [ with_nan ~ 0.0 ^ ! missing_prop !~ -1.0 & missing_prop != -1 ] : u32_count / missing_prop =@
@ cell : i16_age % Clamp low 1_000 high -32768 >> Min
@ batch [ ! donor > 4294967295 ^ donor > .5 ^ missing_prop < 1e10 ] : donor % Convert type Char >> Median type int8
@ cell : ties_int / with_nan =@ >> StdN eps -32768 || 5.
@ cell @ cell :: distance >> GeoMean
@ cell @ gene [ i16_age != 0 & missing_prop <= 2147483647 ] :: UMIs
@ cell @ gene [ with_nan ~ 4294967295 ^ subtype != '' ] :: frac % Clamp high nan >- Mean type Float64
. intver % Log eps 127 % Fraction type Bool
@ cell : type =@ : color =@ / batch >> VarN type bool eps 65535
@ cell @ gene [ missing_prop ~ 5. ] :: frac >> VarN type NotAType eps -128 || 255
@ cell @ gene :: UMIs || 0x10 Symbol
@ cell @ gene :: sparse_umis >- Mode
@ cell @ gene :: sparse_umis % Clamp low 127 high 0x10 >| Var type bool
@ batch : donor % Clamp low .5 / donor
@ cell @ gene :: sparse_umis >- Mean type String
@ cell @ gene :: frac % Log base -32768 eps 100 >| VarN type Int64 eps 32767 || 0b10
@ cell @ cell [ subtype !~ V | missing_prop < 65535 ] :: distance % Convert type Bool % Round digits 0
@ gene [ is_lateral > 0 ] : missing_prop % Convert type float32 % Fraction type Symbol / is_lateral || 127
@ cell @ gene [ missing_prop > inf ^ label ~ V ] :: UMIs >- Min
@ cell @ gene :: frac || 4294967295 float32
@ cell @ gene [ ! ties_int != 1e308 ] :: sparse_umis % Significant high 10 low Inf
@ type : ?
@ batch : missing_prop / donor =@
@ cell @ gene :: sparse_umis % Log eps 100
@ batch [ donor > 1 ] : donor % Abs / donor >> Max
. title || 1e10 % Convert type float32
@ cell @ gene :: frac % Significant
@ cell [ ! batch !~ W ] : subtype / all_nan >> Std
@ gene [ marker <= 1 ] : is_lateral >> Min
@ type : missing_prop >> Std
@ batch : donor / donor >> VarN eps 1 || 1e308 bool
@ cell [ missing_prop ~ Inf & all_neg <= false ] : age || 1
@ cell : type >> Min || 0b10 Int8
@ type [ color ~ 128 ] : color % Convert >> Max
@ cell @ gene [ with_nan = 128 & ! is_doublet != Inf ] :: UMIs
@ cell @ gene :: sparse_umis >> Sum type UInt64 || ''
. title || 1e10
@ gene [ ! missing_prop !~ 1e308 | missing_prop !~ -32768 | marker ~ 5. ] : marker % Log base 1e-10
. version || 4294967295 UInt8
@ batch [ donor ~ 128 & donor !~ 255 ] : donor % Clamp low 128 high 0b10 / donor =@ || 1e-10
@ type [ missing_prop ~ 32767 ] : color >> Min
@ cell : neg_age % Clamp high pi / age >> Median
@ gene [ missing_prop <= NaN ] : is_lateral
@ cell @ cell :: distance >- Mean type Int16
@ batch [ ! donor = -Inf ] : missing_prop
@ type [ missing_prop >= 1_000 ] : missing_prop % Significant low pi / color =@ >> Mean type Bool
@ batch [ donor = 1e-10 ] : missing_prop % Round digits 1 type UInt64 >> Std type UInt8 || -32768
@ cell @ gene :: sparse_umis >- GeoMean
@ cell [ all_nan >= Inf | missing_prop != 4294967296 ] : subtype % Log base 0x10 eps 1e10 % Clamp low 0.5 high -1
. intver % Round
@ gene : marker % Round digits 0
@ cell [ age >= NaN ^ with_nan ~ nan ] : i8_age % Convert type bool / all_zero || 'pi'
@ batch [ ! missing_prop > 0b10 ] : missing_prop % Fraction / donor || 4294967295
. flag || 'a b'
@ cell : type % Clamp low 65535 high -1.0
@ cell [ type !~ V | is_doublet = 0 ] : subtype % Abs type Float64 % Fraction / is_low >> Median
@ cell @ cell :: distance % Convert type Float64 >> Std type int8
@ type [ ! color != -1 ] : color % Convert type int8
@ cell @ cell :: distance >- Std type UInt64
@ cell @ gene :: frac % Log base 1e308 eps 0 >> VarN eps 1
@ batch : missing_prop >> Std || -1 UInt8
@ batch [ ! donor >= 4294967296 ] : missing_prop % Log base 5. >> Count || 1e308
@ gene : is_lateral / is_lateral =@ >> Median type NotAType
@ cell : missing_prop % Round digits 1 type Char / u32_count >> Quantile
@ batch : donor % Convert type bool / donor =@ || NaN
@ type : color || 256 UInt32
@ cell @ gene [ all_zero ~ 32767 | ! infs >= false ] :: sparse_umis >- Mode
@ cell [ batch >= a b & batch != b1 ] : u32_count % Clamp low pi high 128 / batch
@ cell [ ! type = a b ] : ties_int % Round digits 2 type float32 / all_nan
@ cell @ cell :: distance >| GeoMean
@ cell @ gene :: UMIs >- Min
@ cell : label / all_neg =@
@ cell @ gene :: UMIs % Convert type UInt64 >| Mode
@ cell : type
@ cell @ gene [ is_low !~ -1.5 ] :: sparse_umis >> VarN type UInt64 eps -32768
@ batch [ donor != 4294967296 | ! donor !~ -32768 ] : donor % Fraction type Char >> VarN
@ cell @ gene [ ! missing_prop >= 2147483647 ] :: frac % Log eps -1.0 >| Std || 1
@ type [ missing_prop !~ 255 ] : missing_prop / color
@ cell [ u32_count ~ -1 ] : ties_int
@ cell @ gene [ missing_prop > Infinity ] :: frac % Log base .5 eps 65535
@ batch : donor / donor >> Sum type Char
@ type [ missing_prop <= 0.5 & missing_prop < .5 ^ missing_prop != .5 ] : missing_prop / color =@ >> GeoMean eps 0.0 || '0'
@ cell @ gene [ ! type = Z ] :: sparse_umis >- Quantile p 0.25
@ cell @ gene :: UMIs
. title || .5
. title || 1.0 NotAType
@ cell @ gene :: frac >> Std type UInt32 || 2147483648
@ cell @ gene [ with_nan ~ 10 ] :: UMIs % Abs type UInt32 % Clamp high -1.0 >| Sum type Int32
@ type [ ! color < 32767 | ! color > 256 | ! missing_prop != nan ] : color % Convert / color =@ || .5 Int8
@ type [ missing_prop = 255 & color = 127 ] : missing_prop % Significant high 4294967296 low -Inf / color >> Median type float64
@ cell @ gene :: UMIs >> Min
@ cell @ gene :: frac % Fraction type UInt32 >> Median type UInt64
. version % Log base -128 eps inf
@ gene [ marker = 256 ^ missing_prop !~ 5. ] : is_lateral / is_lateral >> Max
@ cell : is_low % Fraction type Int16
. flag % Round digits 2
@ batch [ ! donor >= 2147483648 | missing_prop <= .5 ] : donor / donor >> Sum type UInt32
@ cell : all_neg / subtype >> Mode
@ batch [ missing_prop <= 65535 ] : donor / donor >> Mode
@ cell : ties_int / i16_age =@ >> GeoMean
@ type : color % Round digits 2 type NotAType
@ gene : missing_prop % Abs type float64 >> Max || 1e308
@ cell : all_nan % Convert type UInt8 % Abs type float32 >> StdN
. flag || 1e10 Int64
@ gene : missing_prop / is_lateral =@
@ batch [ missing_prop = 65535 & donor !~ 0.0 ] : missing_prop / donor >> Median type UInt64
@ cell : i8_age / neg_age || 'true'
@ type : color >> Mode
@ cell [ all_zero !~ 5. ] : infs >> Median || 32767 Float32
@ gene [ missing_prop <= nan ^ missing_prop <= inf ] : is_lateral / marker >> Min
@ cell : missing_prop / is_low
@ cell @ gene [ ! ties_int >= 1e10 ] :: sparse_umis >- Max || 256
@ cell @ cell [ ties_int ~ 65535 ^ label ~ Z | neg_age !~ Inf ] :: distance >> GeoMean eps NaN
@ batch [ ! donor < inf | donor = 256 ] : missing_prop || Inf
@ batch [ ! missing_prop = 0.0 ] : missing_prop || 2147483647
@ cell : is_low >> Std
@ gene [ missing_prop !~ 4294967296 | is_lateral >= 1e10 ] : marker / marker
@ batch [ ! missing_prop !~ 0.0 ] : donor / donor >> Mode
@ cell [ ! age !~ inf & all_zero >= Infinity ^ with_nan >= nan ] : age % Abs type Int8
@ cell : is_doublet / is_low =@ >> VarN type Int16 || 32767 Int64
@ cell @ gene :: sparse_umis % Significant high inf low inf >| Max
@ batch : donor / donor >> VarN
@ cell : missing_prop % Abs / is_low >> Min
@ cell [ ties_int > -1.5 & subtype <= Z ] : infs % Log eps 127 / ties_int =@ >> Max
@ batch [ missing_prop != nan ] : donor
@ gene : marker >> GeoMean eps 1e10 || -128
@ cell @ gene :: UMIs >- Sum type UInt8
@ cell [ ! type ~ U | i16_age >= -1.0 ] : neg_age
@ batch [ missing_prop >= 1e-10 ] : missing_prop % Round digits 1 type UInt16 >> Std type Bool || 1e308
@ cell @ gene :: UMIs >| Count
@ gene : marker % Log eps -Inf || 2147483647
@ batch [ donor <= -Inf ] : missing_prop || 256
@ cell @ gene :: frac || -128 String
@ cell : all_nan / with_nan >> Var
@ gene [ marker >= 0.0 ] : missing_prop >> VarN
@ cell [ score != -128 ] : batch % Fraction / batch =@ >> Median type UInt32 || 'true'
@ cell @ gene :: sparse_umis % Abs
@ cell [ label > W ] : batch / score
@ batch : missing_prop % Clamp high 1e308 >> VarN eps nan
@ type [ ! missing_prop <= -128 ] : color % Convert type int32
@ cell [ ! i16_age = nan ] : age >> Max
@ cell [ infs !~ NaN | type ~ '' ^ u32_count < -1.0 ] : missing_prop % Significant high 10 low 10 / f32_score =@
@ batch [ donor = 0b10 ^ donor >= 1.5 | donor !~ 1e-10 ] : missing_prop % Abs type UInt64 >> Var type Int16
@ cell @ cell :: distance % Convert type UInt64 >> VarN
. missing_scalar || 0
. intver || 0
@ batch : donor >> Mean type Int8 || 0.5
@ gene : missing_prop >> Mean type Bool
. missing_scalar || 1.0
. title || 32767 Bool
@ cell @ cell [ subtype >= '' | score !~ -32768 ] :: distance % Clamp low 127 high -1.0 % Abs type NotAType
@ type : color
@ cell [ label !~ V ] : missing_prop >> Std
@ cell : with_nan
@ cell : age
@ type : color % Abs / color >> GeoMean eps pi || inf bool
. version || -32768 Float32
@ cell : subtype =@ : donor % Log eps 1e-10 >> Median type String
@ cell [ neg_age != 256 ^ ! with_nan = 10 ] : infs % Convert type UInt32 / i16_age >> Std type NotAType
@ cell [ all_nan >= 1e10 ] : label
@ cell @ gene :: frac >| Var type NotAType
@ cell @ gene [ subtype > a b ] :: frac % Convert type Bool >> StdN
@ type : missing_prop % Fraction type Int32 / color || 255
@ cell @ gene [ missing_prop < nan ] :: UMIs
@ cell : u32_count
@ cell : type % Log base 4294967296 eps 128 >> Count type UInt32
. title || 'NaN'
@ gene [ ! missing_prop > NaN ] : missing_prop / marker
@ type : color >> Var type NotAType || 0.5 Float64
@ cell : i16_age >> Quantile
@ cell @ gene :: sparse_umis >> Count type Int64 || 65535 bool
@ type : missing_prop % Significant high 1 low -32768 / color
@ cell @ gene :: UMIs >- VarN type Char eps nan
@ cell @ gene :: sparse_umis >| Mean type Bool
@ cell @ cell :: distance >- Median
@ type [ ! missing_prop ~ 5. ^ ! missing_prop < 4294967295 ^ color !~ 4294967296 ] : missing_prop / color >> Min
@ cell [ u32_count !~ inf | is_doublet ~ 128 ] : is_low % Abs type String
@ batch [ missing_prop >= Inf ] : missing_prop / donor >> Mean
. intver || 2147483648 NotAType
@ type [ color ~ -Inf ^ missing_prop != 5. | missing_prop ~ -128 ] : color || -1.5 int32
. version || -1.0
@ cell @ cell :: distance || 'NaN'
@ cell @ gene :: UMIs % Clamp low Infinity high 0x10 >- VarN type Int32 || 'hello world'
@ gene : is_lateral % Fraction >> Sum type String
. title || 255
@ cell [ i16_age !~ 0 | i16_age < 1.0 ] : batch % Fraction type int8 >> StdN || 2147483647
@ cell @ gene :: sparse_umis % Log eps 1e308 >> Mean
@ cell @ gene :: UMIs % Clamp low pi high 128 >| GeoMean eps 2147483648
@ cell [ batch <= '' ] : all_nan || 128
@ cell : age / batch >> Quantile
@ cell [ ! all_nan !~ NaN ^ is_low ~ -1 ^ age != 0.5 ] : ties_int
@ batch [ donor !~ 127 & ! missing_prop < 1.5 ] : missing_prop % Round digits 3 type Float32 / donor =@
@ cell @ gene :: frac >- Mode
@ cell @ cell :: distance >- Median type float64 || 4294967296
@ cell : batch
@ cell : subtype || 1 int32
@ cell @ gene [ f32_score > 10 ] :: sparse_umis >- Mean || 1.0 UInt64
@ cell [ batch >= a b ] : i8_age / label >> Median
@ cell [ ! type < b1 ] : type % Convert type Int16
@ cell @ gene :: UMIs % Fraction >> GeoMean eps 1e308 || -128
@ cell [ i16_age > 1_000 | is_doublet !~ 0 ] : u32_count % Significant high 10 low 1e-10 / i16_age =@ || 1.0
@ batch : missing_prop % Fraction type float32 >> Min || 10 UInt32
@ cell : is_doublet % Log eps 2147483648 / all_nan >> Mode
. missing_scalar || e Bool
@ cell [ score ~ 1_000 ] : all_zero % Fraction / score
@ cell @ cell :: distance % Log base 1e-10 eps pi || Infinity
@ cell : subtype =@ : donor / neg_age =@ >> Std || pi Float32
@ cell @ gene :: sparse_umis % Convert
@ cell [ with_nan = Inf ] : age % Clamp low inf high NaN / i16_age
@ gene [ marker > NaN | is_lateral > 127 ] : marker / marker
@ gene : is_lateral >> Median || 255
@ cell @ gene [ subtype <= '' ] :: sparse_umis >| Var type UInt64
@ cell @ gene [ score <= 1.0 & subtype = U ] :: UMIs >> Count
@ gene : missing_prop % Round type float64 / marker >> Quantile
@ cell [ i16_age < -Inf ] : is_low / all_zero =@ >> Mean type Char
@ cell : missing_prop % Log base NaN eps 1e-10 || 10 int8
@ batch : missing_prop % Clamp low 128
. flag || 1.0 Int32
@ cell : type =@ : color / i16_age
. missing_scalar % Convert type float32
@ cell [ ties_int <= .5 | label < '' ^ ! missing_prop <= 127 ] : f32_score % Abs type UInt64 >> Max
@ cell : ties_int
@ cell @ cell [ missing_prop > Inf ] :: distance % Fraction type float64 >| Count type int8
@ batch : missing_prop % Clamp low 2147483647 high 2147483647 >> Var type String
. flag || 1.0 Int16
@ cell [ ! u32_count = 0.5 ] : subtype =@ : donor =@ % Fraction || ''
@ cell : score % Log base 65535 eps -1.0 / age =@ >> Mean type Char
@ cell : type >> Count
@ type [ color <= 1_000 | color <= 1e10 ] : color % Clamp low 0.0 % Significant
@ cell [ i8_age > -1.0 ] : f32_score
@ cell @ gene [ ties_int >= Inf ] :: frac % Convert type int32
@ cell : ties_int >> Quantile p -0.1
@ gene [ missing_prop <= 0 ] : is_lateral / is_lateral >> GeoMean
@ cell @ gene :: sparse_umis >| Var
@ cell @ gene :: sparse_umis % Abs type Float32 >- GeoMean
@ type [ missing_prop !~ Inf ] : color % Abs / color =@
@ cell : i8_age % Convert type Int64 >> Max
@ type [ missing_prop !~ 65535 & missing_prop <= Inf ] : missing_prop % Clamp low 255 high 0.5 >> Var
@ cell [ neg_age !~ -Inf ] : score % Clamp low -1 high 1_000 >> StdN type Int8
@ gene [ ! is_lateral >= 1.5 ] : missing_prop % Significant / is_lateral
@ cell @ gene :: UMIs >| StdN type String || 0b10
@ cell [ with_nan < -Inf ] : neg_age % Convert type Char / type =@ >> Std
@ cell @ cell :: distance >> VarN type NotAType eps -1
@ cell [ is_doublet !~ 255 ] : all_neg || 'true'
@ cell @ gene [ ! u32_count != 1e308 ] :: sparse_umis % Clamp high 2147483648 >- Var
@ cell @ cell :: distance >| Count
@ type [ color ~ 0.0 ] : missing_prop % Round digits 0 type Float64
@ batch [ ! donor = 256 ^ missing_prop >= 1.0 ] : missing_prop / donor || 1.0
@ cell @ cell [ with_nan !~ 0x10 ] :: distance % Log base Infinity eps 32767
@ cell @ cell :: distance >- Sum type Int32 || '0'
@ cell @ gene :: frac || e
. version || 0.5 int8
@ type : color / color =@
@ gene [ is_lateral > -1 ] : missing_prop / marker =@ || 'a b'
@ cell : i16_age % Log base 1e-10 eps 4294967296 >> Var
@ cell @ cell :: distance % Clamp low 0 >- Median || -Inf UInt64
@ cell @ cell :: distance % Round type Int8 >> StdN type UInt64
@ cell : batch % Abs type bool
@ cell @ cell :: distance >- Min
@ cell @ gene :: sparse_umis % Fraction type String >> VarN type Int64 eps 0.0
@ cell : score / subtype
@ cell @ gene :: frac >> StdN type UInt64 eps 2147483648
@ cell : all_zero / type
@ gene [ is_lateral = 100 ] : marker % Convert >> StdN
@ cell @ gene [ batch ~ '' ] :: UMIs
@ gene : marker || 1_000
@ cell @ cell :: distance % Round
@ cell [ score ~ -32768 ] : all_neg % Clamp low 1e308
@ type : missing_prop % Abs type NotAType
@ cell @ gene [ i16_age ~ 10 ^ ties_int < 1e-10 ] :: frac % Log base -128 eps 0.5 >> Min
@ cell @ cell :: distance >| Mode
@ cell : i16_age % Significant low Inf >> Median type Bool
@ cell [ f32_score !~ 4294967296 | all_nan = 255 ] : is_doublet / all_nan >> Min
@ cell [ score ~ -1.0 & neg_age < 0 ^ ! f32_score != 256 ] : label / is_doublet || 32767 UInt16
@ batch [ ! missing_prop ~ 1.5 & missing_prop ~ 1 ] : missing_prop % Log base 1.5 eps nan / donor || 4294967296 float32
@ cell @ gene :: sparse_umis >- Min
@ cell : i16_age % Significant low 1e308 / infs
@ cell @ cell :: ?
@ cell [ is_low <= -1.0 ] : ties_int >> Median type UInt16 || 1_000
@ gene [ ! marker = .5 ^ is_lateral <= 128 ] : is_lateral % Clamp low 4294967295 high pi % Round type bool / marker
@ cell [ infs > .5 ^ u32_count >= 255 ] : score >> StdN || -128
@ cell : all_neg % Convert % Abs >> Var type Float64
@ cell [ ties_int < -1 | ! i16_age > 0x10 | ties_int !~ Infinity ] : infs % Convert type NotAType
@ cell @ gene :: UMIs >| Mode
@ batch [ missing_prop != -1.0 ^ ! donor != 128 ^ donor = -Inf ] : donor % Clamp low pi / donor =@
@ cell [ batch ~ b1 | u32_count !~ 1_000 ] : missing_prop % Fraction type Int8 / ties_int =@
@ cell @ gene :: sparse_umis % Fraction type Char >> Var type Int8
@ cell [ ! all_nan > 1 & all_neg <= -1.0 ] : all_neg % Log base 4294967296 eps 127 / subtype =@
@ cell @ gene :: sparse_umis % Log base 65535 eps 100 % Convert type UInt64 >> Median type Float64
@ cell @ gene :: sparse_umis % Convert type UInt32 >- Max
@ cell @ gene [ batch != V ] :: UMIs >- Mode
@ cell @ gene :: UMIs % Fraction type Bool
@ cell [ all_neg > 1e308 & all_nan = 128 & age !~ 1.0 ] : with_nan / batch =@
@ batch [ missing_prop !~ 0.5 ^ missing_prop <= -128 ] : missing_prop >> Count
@ cell [ type >= a b | age <= 0 ] : all_nan % Log base 5. eps 127 % Round digits 0 type float64
@ cell : with_nan % Log base NaN eps 5. >> Quantile p 0.5 || 5. NotAType
@ cell : f32_score % Log base pi eps 1e10 >> Mode
@ cell [ is_doublet <= 65535 & batch >= V ] : type =@ : color
@ batch [ donor >= NaN & donor ~ -32768 ] : missing_prop % Abs / donor
@ batch : missing_prop % Round digits 3 type int8 / donor
@ cell : i8_age
@ type [ color >= 1_000 ] : color % Log base -1.0 eps inf
@ cell : infs / age >> VarN
@ cell @ cell :: distance % Convert type float64 >| StdN type UInt16 || 2147483648 UInt8
@ cell @ gene :: sparse_umis % Clamp high 1_000 >> Max || Infinity
@ gene [ ! marker ~ 1.5 ] : missing_prop / is_lateral =@ >> Max || 0.5 Bool
@ type : missing_prop % Round digits 3 type Float32 / color || 1_000
@ gene : is_lateral % Fraction type Int32 / marker =@ || Inf
@ cell : is_low % Significant high 0.0 low 1e10 / all_zero
. title % Convert % Clamp low 5. high 1e308
@ cell @ gene :: frac % Fraction
@ cell [ batch != b1 ^ ! label != W ] : label % Clamp high 5. >> Min
@ cell [ type = W ] : infs >> Std
@ cell [ ! i8_age >= 1_000 ] : ties_int || ''
@ cell [ all_neg !~ 0 | ! all_neg <= nan ] : all_neg / f32_score =@
@ type : missing_prop / color
@ gene : missing_prop % Abs type int8 % Abs type float64 >> Max
@ gene [ marker ~ 1.0 ] : marker >> GeoMean
@ gene [ marker ~ Inf | missing_prop <= 2147483648 & marker >= NaN ] : marker
@ cell @ gene :: frac || 'true'
@ gene : marker % Fraction
@ cell : infs / subtype >> Quantile p 0.75
@ batch : donor
@ cell [ ! all_nan ~ 1e-10 ] : neg_age / all_nan
@ gene [ missing_prop !~ 2147483648 & missing_prop != -1 | is_lateral !~ 1_000 ] : is_lateral / is_lateral =@
@ cell : is_doublet % Fraction type bool / type >> Min
@ batch [ missing_prop ~ 32767 | ! donor = nan | ! donor = 127 ] : donor >> Sum
@ cell [ f32_score ~ -1.5 ^ ! ties_int ~ 0 ] : i16_age % Significant high 1e-10 low 255
@ cell @ gene :: UMIs % Clamp
@ cell @ gene :: frac % Abs type Bool >| Count type Char
@ cell : all_zero % Convert type UInt32 || Infinity
@ cell @ gene :: frac % Fraction type UInt64 >| Median type Symbol
@ gene : marker
@ cell : neg_age % Log base 1e308 / is_doublet >> Min || 0.0
@ cell @ cell [ i16_age < 1e-10 & u32_count !~ 65535 ] :: distance
@ cell @ cell [ infs !~ 0.5 ^ score >= 0x10 ] :: distance >- VarN || 'NaN'
@ cell [ age >= 1 | missing_prop != 0.0 ^ i16_age >= true ] : all_neg / f32_score
@ gene [ ! marker <= 32767 ] : is_lateral / marker =@ >> Mean type UInt32
@ cell [ ! i8_age ~ 65535 ] : score / is_low >> StdN type float32
@ cell @ gene [ ! batch ~ a b & with_nan < 4294967295 ] :: UMIs >| Var type int32
@ gene [ marker > -1 ] : is_lateral >> Max || 127 UInt16
@ cell : all_nan % Fraction / i16_age =@ >> Median type int32
@ type [ color < 0.0 ^ color <= 2147483648 ] : missing_prop % Significant high -128 low 0
@ cell [ with_nan <= 65535 ] : with_nan / all_neg =@ || 2147483648
@ cell : all_nan % Convert type float64 / score >> Mean type Char || NaN
@ type [ missing_prop > Inf ] : missing_prop
@ batch [ missing_prop > 0b10 ] : donor % Log eps 127
@ cell @ gene :: sparse_umis % Convert type Symbol % Round type Float64 >| Max
@ cell : is_low >> GeoMean
@ cell [ ! i8_age <= 1.5 & all_nan <= 1 ] : batch / i8_age >> GeoMean || -32768
@ gene : missing_prop % Convert type Bool / is_lateral || inf Char
@ cell [ u32_count ~ 1 ] : score % Log base 255 eps pi % Clamp low inf high 2147483647 >> Std type UInt64
@ type : missing_prop || -1.5
@ gene : is_lateral >> Mean
@ cell [ all_zero >= 2147483647 ^ ! label < W ] : type / is_low =@
@ gene [ ! is_lateral < 1e308 ^ ! is_lateral <= 1.0 ] : missing_prop % Convert type Float32 % Clamp low 32767 high 2147483648
@ cell [ i8_age < 10 ] : all_zero % Significant low 1_000 / all_nan =@ >> Mean type UInt32
@ type [ missing_prop > 4294967295 ] : color >> Max
@ cell @ cell :: distance >| Var type Int32
@ cell [ ties_int <= -32768 ] : age / infs =@ >> Count
@ gene [ missing_prop > 32767 & marker > 2147483647 ] : marker >> Mean
@ cell @ gene [ ! with_nan >= 0.5 ] :: frac % Fraction type UInt32 >> Var type UInt32
@ cell : i16_age / all_nan =@
@ cell @ cell :: distance % Round digits 0 >> Count type Float64
@ gene : missing_prop / marker >> Mean
@ cell [ is_doublet ~ -32768 | missing_prop = 1.0 ] : batch / infs
@ type [ missing_prop >= 1.5 ^ ! missing_prop != -Inf ] : color % Abs type Int16
@ type [ color ~ 1e308 ] : missing_prop
@ cell : age % Round digits 3 type Float32 >> VarN type float64
@ cell : age % Abs type UInt64 >> Std type Int64
@ batch : ?
@ batch : missing_prop % Log base 10 eps -128
@ type [ missing_prop !~ -1.5 ] : missing_prop >> Mode
@ cell : i8_age % Log base 32767 >> Mode
@ type [ ! missing_prop <= 4294967296 & missing_prop < 1e308 ] : missing_prop % Fraction >> Var
. missing_scalar || -1.0
@ cell @ gene :: UMIs >- Count
@ type [ missing_prop != 1.5 ] : missing_prop / color =@
@ batch : donor / donor =@
@ cell : i16_age % Log base 0x10 eps 128
@ cell : u32_count % Log base 128 eps 256 >> Mean type UInt16 || 5. Bool
@ type : missing_prop / color >> Max
. missing_scalar || 5. float64
@ cell : batch % Clamp
@ cell @ gene :: UMIs >- Std
@ cell : f32_score % Log base 1_000 eps 0.5 % Log
@ gene : ?
@ cell @ gene :: UMIs >- GeoMean
@ gene @ gene :: ?
@ cell @ gene :: UMIs % Fraction >- Std
@ batch : donor >> Mode
@ type [ missing_prop <= 32767 | missing_prop !~ 100 ] : missing_prop % Log / color
@ cell [ score >= 0 | ! infs != 65535 ] : label % Clamp low 128 high -1.0 / f32_score
@ cell @ gene [ missing_prop ~ 0x10 ] :: sparse_umis % Round digits 3 type UInt64 >| Sum type Float32
@ batch [ missing_prop = inf ] : donor >> Mean type String
@ gene [ missing_prop !~ 256 ] : marker % Abs type Char % Fraction type Symbol
@ cell [ type <= a b ] : is_doublet / infs >> Max || inf
. missing_scalar % Convert
@ cell : subtype >> Var type Int64 || 4294967296
@ cell @ gene [ all_neg != 4294967296 ^ age > 5. ] :: frac % Convert type float32
@ cell : f32_score
@ cell @ cell :: distance % Clamp high -32768 >| Std type Symbol
. version || 1.0 Int8
@ type : color % Log eps 0x10 >> Min
@ cell @ gene :: UMIs % Convert type String % Clamp low 4294967295 high 1e308
@ cell @ cell [ type !~ V ] :: distance
@ cell [ score = 2147483648 & f32_score > -Inf ] : ties_int >> Max
. missing_scalar || ''
@ cell @ gene :: frac % Clamp low 100 high 127 >> Max
@ cell : u32_count / i16_age >> Sum type int8
@ cell : all_neg % Round digits 2 type Symbol % Round type UInt32 || -1.5
. intver % Clamp low 1e10
@ gene [ missing_prop > 2147483648 & is_lateral = 1_000 ] : marker % Significant low Inf % Significant high nan / marker
@ cell : ties_int % Log base -1 eps 127 >> Min || 1.5
@ cell [ i8_age ~ true | ! is_low !~ -1 ] : infs % Convert % Significant high 1 low inf / type
@ batch [ donor !~ -1.5 ] : missing_prop
@ cell @ gene [ f32_score <= -32768 ] :: frac % Abs >> Mean
@ cell [ ! ties_int = Inf ] : batch >> GeoMean eps 1e308
. intver || -Inf
@ cell @ cell [ all_nan != 1 ] :: distance % Fraction type UInt16 || 1
@ batch : donor / donor
@ type [ color > Infinity ] : missing_prop
@ cell [ missing_prop ~ 2147483647 ] : subtype =@ : donor % Clamp low 65535 high 1.0
@ cell : infs % Convert type float64 >> Std || -1.0
@ cell @ gene :: UMIs >> StdN type String
@ gene [ marker > 255 ] : missing_prop / is_lateral =@ || 'pi'
@ gene : missing_prop >> Median
@ cell @ gene :: sparse_umis >| VarN eps -1.5
@ cell @ gene :: sparse_umis % Round type String >> Min || 'NaN'
@ cell @ cell :: distance % Round type int8 >- Count
@ type [ missing_prop != 100 ^ missing_prop != NaN & color !~ .5 ] : missing_prop % Clamp low -32768 high 65535 / color
@ cell [ ties_int != 0 | ! label = U ^ score >= true ] : label / is_doublet
@ cell @ cell :: distance % Clamp high 1.0 >> Min
@ cell @ gene :: frac || 1 String
@ cell [ missing_prop > 1.0 | missing_prop = 2147483648 ] : ties_int || 128 int32
@ type [ color != 127 ^ color > nan ] : color % Log base Inf eps 1e308 / color =@
@ batch [ missing_prop >= 128 ] : missing_prop
@ cell [ is_low = 1 ] : is_doublet % Significant high 1e-10
. version % Convert type bool
@ cell [ ! neg_age !~ 1 ] : f32_score / with_nan
@ cell [ label > b1 | missing_prop > 2147483648 ] : is_low / neg_age
@ cell : ties_int / type >> Mean type Int64 || 255 Float32
@ cell : all_nan % Abs type float32 / with_nan >> VarN type int32 eps 1.5
@ cell @ gene :: UMIs % Clamp low inf >> Mode
@ cell [ i16_age >= 2147483648 & is_low !~ -128 ] : with_nan % Fraction / is_low =@
@ cell [ f32_score ~ 1e-10 ^ ties_int !~ NaN | batch !~ W ] : type % Clamp low .5 / all_nan =@
@ cell @ gene [ ! all_nan !~ 1e-10 ] :: frac % Abs type NotAType >> VarN type Symbol eps -1
@ cell [ label != V ] : all_nan || 255
@ batch : missing_prop >> Max
@ cell @ gene :: UMIs >- GeoMean eps -Inf || inf
@ cell : is_doublet / i8_age =@
@ cell : with_nan >> Mean type Symbol || Infinity Float64
@ batch : missing_prop || -Inf Int32
@ cell [ with_nan <= 4294967296 ^ all_zero != -1.5 ^ ! u32_count > 127 ] : neg_age / batch =@ >> Sum type int32
@ batch : missing_prop || 256 float64
@ cell @ gene :: frac >| Var || 4294967296 Int32
@ gene : missing_prop / marker =@ >> Std type String || pi int8
@ cell @ gene :: UMIs % Fraction type Int8 >- VarN type float64
@ cell : i8_age >> GeoMean eps 10
@ gene [ is_lateral >= 4294967296 ] : is_lateral / marker >> Mean
@ cell : f32_score / all_zero
@ type [ missing_prop >= 10 & ! missing_prop != 2147483647 ] : color % Fraction type Float32 / color =@ >> Mode
@ cell [ missing_prop = 127 ^ label ~ '' | infs >= 4294967296 ] : score >> Median type float32
@ cell [ subtype ~ W ] : all_nan / age || 1.5 bool
@ batch : donor % Fraction type UInt32 % Fraction type Float64
@ batch [ ! donor ~ 0x10 ] : missing_prop / donor
@ gene : marker / is_lateral >> GeoMean eps 1e-10
@ cell [ f32_score = 1_000 ^ subtype <= W ] : infs || Inf
@ batch : missing_prop / donor >> Sum type NotAType
@ cell @ cell :: distance >- Count
@ cell : age / label >> Sum
@ cell : batch >> Quantile
@ cell [ label = '' & ! all_neg <= 0x10 ] : missing_prop % Abs type int32 / all_neg >> GeoMean || -Inf
@ batch : missing_prop >> Sum type Symbol
@ cell [ ! is_low >= 100 ] : batch / neg_age >> StdN eps -Inf
@ gene [ is_lateral != 0x10 | is_lateral > 0x10 ^ marker !~ -1 ] : missing_prop
@ batch [ missing_prop <= 255 ] : donor / donor =@ >> GeoMean eps 1.5 || -Inf
@ gene [ marker != 65535 & is_lateral < 2147483647 | missing_prop < 128 ] : marker / is_lateral =@ >> Median
@ type [ color > 0b10 | missing_prop > 0.0 ^ color ~ 5. ] : color / color =@ >> VarN eps 1
. title % Log base 1e10
@ cell : infs % Convert >> GeoMean eps 127
@ cell @ gene :: UMIs % Convert type UInt16 >- VarN type UInt64 eps -1.5
@ type [ color <= 65535 ] : missing_prop >> Mode || 4294967295
@ cell @ gene :: sparse_umis >| Max
@ cell @ gene :: UMIs >| Max
@ cell @ cell [ with_nan != 2147483647 & neg_age < NaN ] :: distance
@ cell @ gene :: frac % Fraction type int8 >| Median type bool
. intver || -1.0 UInt64
@ batch : donor % Significant high 2147483647 low nan % Round >> Sum
@ cell [ missing_prop = 65535 ] : all_neg % Round digits 1 type Char / neg_age >> GeoMean eps pi || -1.0 Int8
@ cell @ cell [ type >= a b | u32_count = 1 ] :: distance % Abs type Int64 >- Max
@ cell : all_neg % Abs / f32_score =@ >> GeoMean
@ gene : missing_prop / is_lateral || 2147483648
@ batch : missing_prop % Fraction type String / donor >> Max
@ type : missing_prop % Log base 1 >> Mode
@ type [ missing_prop < 0b10 & color !~ -Inf ] : missing_prop / color =@
@ cell @ gene :: UMIs % Convert || nan UInt64
@ cell @ gene :: UMIs % Fraction
@ cell @ gene :: sparse_umis >| Mean || 4294967296 NotAType
@ cell : label / batch >> Mean
@ batch : donor || e
@ cell @ gene [ missing_prop >= 1 ] :: sparse_umis >| Mode || 'hello world'
@ cell @ gene [ ! i16_age >= 1e10 | all_zero < 0x10 ] :: UMIs % Significant low 1e308 % Significant
@ cell [ score != .5 & all_zero < nan ] : score >> VarN eps -1.0
@ cell @ gene :: sparse_umis || '0'
@ cell @ gene :: frac >| Var type UInt32
@ cell @ gene :: sparse_umis % Log base -1 >> Quantile
@ cell : is_doublet / all_neg
@ cell @ gene :: sparse_umis || 0x10
. missing_scalar || NaN
@ gene : is_lateral
@ cell : label % Convert
@ cell @ gene :: frac >- Std
@ cell : label / subtype =@
@ cell @ gene [ neg_age = 1.5 ^ ! batch <= b1 & ties_int > 1_000 ] :: sparse_umis % Clamp low 1 high -1.5 % Clamp low 255 high 1e10
@ cell @ gene [ label != V ^ missing_prop <= -32768 & ! age <= 0 ] :: frac % Convert type Symbol
@ cell @ gene :: UMIs % Abs type UInt64 >| Std
@ cell : batch =@ : donor % Round digits 2 type UInt16 / all_zero >> Std type UInt32 || 0 Int16
@ cell [ i8_age = nan & neg_age < 4294967296 ] : neg_age % Significant low -1.5 >> VarN type Int32 eps 2147483648 || 'hello world'
@ cell : age % Log base pi eps 2147483648 >> Max || 65535
@ cell : infs % Fraction type NotAType
@ gene [ marker !~ 2147483648 | marker <= 1.5 ] : missing_prop / marker || 'hello world'
@ cell @ gene :: UMIs >> GeoMean eps 256
@ cell @ gene :: UMIs % Convert type float64
@ cell : age >> Mean
@ cell : f32_score % Round digits 0 >> Std type String
@ cell [ missing_prop = 1e-10 ] : missing_prop % Fraction type float32 >> Mode || 127
@ batch [ ! missing_prop >= -Inf | donor !~ 1_000 & missing_prop <= 256 ] : donor / donor =@ || 2147483648
. missing_scalar || 256
@ gene : is_lateral % Clamp high 4294967295 / marker =@ >> Count
@ gene : missing_prop / marker || e Float64
@ gene [ ! marker !~ 1 ] : missing_prop % Significant high 1e308 low Inf / is_lateral >> Var type UInt8 || 10 int32
@ type [ missing_prop ~ 1 | ! missing_prop >= Infinity ] : missing_prop % Clamp high 0b10 >> GeoMean eps 1.0
@ cell : subtype >> GeoMean || 255 float32
@ gene [ ! marker >= 0b10 ] : marker % Significant high inf low 1_000
@ type [ missing_prop ~ 127 | color = 0x10 ^ missing_prop < 0.5 ] : missing_prop % Fraction type NotAType % Log base 128
@ gene [ marker != -1.0 & marker != 1_000 ] : marker / is_lateral >> GeoMean
@ cell [ f32_score < nan ] : all_zero % Fraction type float32 % Fraction || 0b10
@ type : missing_prop % Clamp low e % Fraction || 1_000
@ type : missing_prop % Fraction type Int8 / color =@
@ cell @ gene :: UMIs % Clamp high 1.5 >| Max || 4294967296
@ cell [ is_doublet ~ 0 & ! age ~ NaN ] : subtype =@ : donor % Convert type UInt64 / label =@ >> Mean
@ gene : marker / marker >> Sum
@ cell [ is_doublet !~ 128 ] : age / is_doublet =@
@ cell @ cell [ ties_int <= 0 ^ is_low = 127 ^ with_nan >= 127 ] :: distance
@ gene [ ! missing_prop <= 0.0 | is_lateral < 1.5 ] : missing_prop
@ gene : marker / marker =@ >> Var
@ cell @ gene :: UMIs >> Std
@ cell @ gene :: frac >- Quantile p NaN || .5
@ cell [ ! i8_age < NaN ^ score >= 0 | ! subtype ~ U ] : infs % Fraction type Bool >> Max
@ cell : batch % Clamp low nan high Infinity % Round digits 3 / missing_prop
@ batch [ donor = 32767 | ! donor <= 2147483648 & ! missing_prop > 1e308 ] : missing_prop
. flag || 255 Int8
@ cell @ gene [ age ~ Infinity ^ infs !~ 1.0 ] :: frac % Round digits 0 type NotAType
@ cell [ ! type > '' ] : i16_age / type =@ || pi
@ cell @ gene [ ties_int > 256 ] :: sparse_umis >- Min
@ cell @ cell [ batch <= '' ] :: distance >| Var type bool
@ type : missing_prop >> Mode
@ cell : score % Round digits 2 type UInt64 >> Std || 0 float64
@ cell @ gene :: frac >| Median type NotAType
@ gene : missing_prop % Round digits 1 type NotAType
. flag || 5. UInt32
@ cell @ cell :: distance >> StdN eps 4294967296 || .5
@ cell @ cell :: distance >> Mean
@ cell @ gene :: sparse_umis >> VarN eps 1.0
@ gene [ is_lateral >= -32768 & ! is_lateral != 1 ] : marker % Significant high -128 >> Quantile p 0.25 || 'a b'
@ type [ missing_prop >= 1e10 & color !~ -128 | missing_prop != -Inf ] : missing_prop % Clamp low 2147483647 high -1.0 >> Std type String
@ cell : missing_prop
@ cell @ cell [ u32_count !~ Inf ^ ! score < 2147483648 ] :: distance
@ gene : marker >> Max
@ cell : type % Fraction type UInt64 / batch
@ gene [ is_lateral >= 2147483647 | missing_prop >= -1 | ! marker <= 1e308 ] : marker / marker || -1.0 Float32
@ type [ color !~ 65535 ] : color
. title || Inf String
@ cell : age % Convert type float64 / i16_age
@ cell @ gene [ ! i16_age <= 1e-10 & i16_age <= -1 ^ all_neg >= 10 ] :: frac >- VarN
@ cell [ all_nan ~ 1e308 ] : i16_age >> GeoMean
@ cell [ f32_score > 4294967295 & all_zero != Inf ] : u32_count || -1.0 float32
@ cell @ gene :: UMIs % Round digits 3 % Log base 10 eps 1.0
@ cell @ gene [ all_nan < true ^ missing_prop > 0.5 ] :: sparse_umis % Log base 1.5 eps 256 >- Sum type Int16 || 255
@ gene : is_lateral % Log base 0 eps 4294967295 >> Mode
@ cell : u32_count % Abs type Bool >> VarN
@ cell : infs / subtype >> Quantile
@ gene : is_lateral % Log eps 255 >> Mode || 0
@ cell : f32_score >> Median
@ gene [ missing_prop > 10 ] : marker >> Sum
@ cell @ gene :: UMIs % Significant high 100 low 0 >> Sum
@ gene [ marker = Inf ] : is_lateral % Significant low 1_000 % Log base 4294967296
@ cell : all_zero % Clamp low 0 high 256 / ties_int
. version || 'pi'
@ gene : marker || 4294967295 float64
. intver || inf bool
@ batch : missing_prop % Convert type Float32
@ cell @ cell [ missing_prop < -1 ] :: distance
@ cell @ gene [ score < 100 & missing_prop > 10 & u32_count ~ 0.0 ] :: frac >- Sum type String
@ type [ missing_prop > 0.0 ] : color % Abs % Convert type bool || 2147483647 float64
@ gene [ is_lateral > nan ] : is_lateral / marker || 5.
. title || inf
@ cell : batch % Fraction
@ cell [ age = nan ] : ties_int % Abs type Symbol / ties_int >> Min || 1.5 bool
. intver || 'true' % Significant high 32767 low 0
@ cell [ type != '' ^ i8_age ~ -1 | all_zero < 0.0 ] : all_neg >> Mode || 127 Bool
@ cell : age % Convert type Int32 >> StdN type Bool
@ cell @ cell [ missing_prop >= 0 | age < 2147483648 ] :: distance || -1.5
@ cell : all_zero % Round / all_zero || 1e308
@ cell @ cell :: distance >> Mode || 'hello world'
@ cell : infs >> Min
@ cell @ gene :: frac % Clamp low 32767 high 100 % Significant high -1 >- Quantile
@ batch : donor >> Median type Int8
@ cell @ gene [ is_doublet > 1.0 ] :: sparse_umis >> Min
@ cell @ cell [ age != 0.5 ] :: distance % Abs type int8 >| Mean type Symbol
@ cell [ ! infs < -1.0 ] : is_doublet / with_nan >> Std type float32
@ cell [ with_nan <= .5 & ! subtype <= '' & all_neg !~ 1.5 ] : batch / neg_age >> Std
@ cell [ ! i8_age >= 0b10 ] : neg_age % Convert type Float64 / type >> Quantile p 0
@ cell @ cell [ all_zero ~ -128 ^ missing_prop > 1.0 ] :: distance % Fraction type int32 % Significant high -1.0 low inf >> Max
@ type : color % Significant high 0b10 % Round digits 1 / color
@ cell [ infs ~ NaN ] : u32_count % Log base 0 eps 5.
@ cell [ is_doublet < 0.5 & all_nan !~ 255 ] : missing_prop % Abs type float64 % Log eps -128 || 0b10 Int64
@ batch [ missing_prop <= 10 ] : donor >> Max
@ cell @ cell :: distance % Convert type Int32 >> Count type UInt8
@ cell @ gene :: UMIs % Fraction type bool
@ cell @ cell :: distance >> GeoMean eps inf
@ cell @ gene :: UMIs % Round digits 3 type Int16 >- GeoMean
@ cell @ gene :: frac % Clamp low 0 high 0.0 >> Min
. title || -1.5 % Convert type Symbol
@ cell [ age ~ 0.0 | infs != -1.0 ] : all_nan % Convert type bool
@ cell @ gene [ ! type <= a b ] :: frac
@ cell : label / type =@ >> Std type Int64
@ cell : label >> VarN type UInt64 eps 10 || 1.0
@ cell [ batch <= V ^ i16_age != -Inf ] : label % Convert >> Min
@ type : missing_prop / color =@ >> Median
@ cell [ all_zero <= 0x10 ] : i8_age / batch >> Sum type String || 1e308
@ cell @ cell :: distance % Fraction type Int32 >| Quantile
. intver || 2147483647 UInt8
@ type : missing_prop % Log eps -Inf % Convert
@ cell @ cell :: distance >- StdN type String
@ cell [ ties_int != 1 ] : all_zero / neg_age >> Mode
@ cell @ gene [ missing_prop <= 32767 ^ type < b1 ] :: sparse_umis % Log base 1.5 eps -128
@ gene : missing_prop >> Var
@ cell [ f32_score ~ 0 ] : age % Log base 0b10 / score =@
@ cell : batch >> Var type bool
@ cell @ gene [ i8_age = 32767 | missing_prop ~ 0.5 ^ infs < NaN ] :: sparse_umis || 1e10
@ gene [ ! missing_prop <= 0b10 ] : missing_prop % Convert type Char
@ cell : infs || 'pi'
@ type [ color = 32767 ] : missing_prop % Log base 1
@ gene : missing_prop % Significant high Inf low 0x10 >> Mean
@ cell @ gene :: frac >> GeoMean eps -1.0
@ cell : is_low % Abs type Int8 / subtype =@ >> GeoMean eps 1
@ cell : f32_score / all_neg =@ >> Min
@ type [ missing_prop ~ 128 ] : color % Convert type Int8 / color
@ type : missing_prop % Abs type UInt16 || -128
. version || 1e-10 Int64
@ cell [ batch <= '' & all_zero ~ 1_000 | ! ties_int >= -1.0 ] : subtype / is_low
@ cell @ gene [ neg_age = 1.0 ] :: frac >- Max
@ batch : missing_prop >> Mode || -Inf
@ gene [ missing_prop !~ 128 ] : marker / is_lateral >> Max
@ cell @ cell :: distance >- GeoMean
@ batch : missing_prop / donor >> Quantile p NaN || Inf UInt16
@ cell : missing_prop % Convert type Bool / age >> Quantile
@ gene [ ! missing_prop <= nan ] : missing_prop % Clamp low 0.0 high inf / marker >> Max
@ gene : missing_prop >> Min
@ cell [ all_zero ~ 0.0 ^ with_nan !~ 100 ] : infs % Abs type Char >> Mean type UInt64
@ type [ color != Infinity ] : color % Fraction type Float64 / color
@ cell [ ! is_low != -1.0 ] : score % Log base 0.5 eps 256 >> Max
@ cell @ gene :: frac % Significant low 1e308 >| Quantile
. flag || 'hello world'
@ batch [ donor >= 0.0 ] : missing_prop % Abs type NotAType / donor >> Mean type NotAType
@ gene [ missing_prop != 10 | is_lateral != 5. ] : missing_prop % Significant high 1_000 low Infinity
@ type : color % Convert type bool / color
@ cell @ gene :: frac || .5
@ cell @ cell :: distance % Abs >> Var type Float32
. title || 0b10 Float32
@ cell : missing_prop / all_zero
@ cell @ cell [ missing_prop >= nan ] :: distance >> Median
@ type [ missing_prop <= Infinity | missing_prop >= 127 ^ ! missing_prop = 5. ] : color / color =@ >> Sum type Float64
@ cell : i16_age / all_neg >> Mode
@ cell : with_nan >> Var
@ type : color % Log eps -1.0 >> Max || 5.
@ type [ color < Infinity ] : color % Convert
. title || Inf
@ gene [ marker ~ 255 & marker >= Inf ] : missing_prop >> Max
@ gene [ missing_prop != .5 ] : missing_prop
@ cell [ missing_prop > 32767 ] : f32_score % Fraction || -128 UInt16
@ cell [ i16_age = Infinity ] : with_nan % Convert type Int8
@ batch [ missing_prop < 0b10 ] : missing_prop % Convert type Int8 / donor =@ >> Std type UInt64 || pi
@ cell : subtype % Convert type UInt8 / neg_age >> Mode
@ cell @ gene :: UMIs % Round digits 2 type Int8 >| GeoMean eps 10
@ batch [ donor != 0 | ! missing_prop !~ 128 | missing_prop = 2147483648 ] : donor % Round type Int32
@ cell @ gene :: sparse_umis >> Var type UInt8
@ cell [ age !~ Inf ] : all_neg % Convert type Int32 / all_neg >> Median type float64 || 'true'
@ batch [ donor < 1_000 ] : missing_prop % Abs type Int16 >> Min
@ cell @ gene :: frac >> Std
@ batch [ missing_prop !~ 4294967296 ] : missing_prop >> StdN
. flag || nan float32
@ type [ missing_prop < 100 ] : color / color =@ >> Mean
@ cell : all_nan % Log base Inf || 1.0 float32
@ cell @ gene :: frac % Fraction type Int8
@ type [ missing_prop <= NaN & missing_prop >= 1e10 ] : color % Abs type Float32 >> Mode
@ cell : all_nan / i8_age || 1.0
@ cell [ score <= 1 | all_neg !~ 256 ] : i16_age % Clamp low 1.5 high 0 >> Var
@ cell : all_zero % Round type UInt8 / neg_age >> Var || Inf
@ cell @ gene :: frac >- Sum type Int32
@ cell : is_low % Convert type String >> Count
@ cell @ cell [ ! i16_age < 127 ] :: distance >> Count type int8
. intver || 10 % Clamp low 1e308
@ gene : missing_prop >> Var || 0.5
@ gene : missing_prop >> Std
@ cell @ gene [ ties_int <= -1.0 & ! all_zero < -1 ] :: frac >> Median
@ cell @ cell [ all_zero = -32768 ] :: distance % Log base inf eps -Inf
@ cell @ gene :: sparse_umis >> Median type Int16
@ cell [ type < a b ^ is_low = 100 ] : missing_prop % Fraction type Int64
@ cell @ gene :: frac >| GeoMean eps -32768
@ batch [ missing_prop != .5 & donor <= inf ] : donor % Round type Int16 >> Count type UInt8 || Infinity
. flag || 1e-10
. intver || 100
@ batch [ ! donor !~ Infinity ] : donor / donor >> Mean type float32
@ cell : label >> Sum type UInt16 || inf Bool
@ type [ missing_prop > 65535 ] : missing_prop
@ cell : with_nan % Fraction type Bool / type
@ cell @ gene [ infs ~ -1.5 & i16_age = 4294967295 ] :: UMIs
@ batch [ missing_prop <= 1_000 | missing_prop !~ 1e-10 ^ donor <= -1.0 ] : missing_prop % Abs || 127 NotAType
@ type : color % Round digits 1 type Int64 / color || 128
@ cell @ gene :: UMIs % Clamp low nan % Fraction type Int32
@ cell @ gene :: UMIs % Clamp low -128 high 4294967296 >> Count type UInt64
@ cell [ type >= '' ^ label ~ a b ] : missing_prop % Significant high 1e308
@ cell [ all_nan < 32767 ^ neg_age != 1.0 & all_zero !~ 1e308 ] : with_nan % Log eps 32767 || -128
@ batch : missing_prop / donor >> Std
@ gene : is_lateral || -128 UInt32
@ cell : neg_age >> Median type Int8
. flag || -1.0 int32
. title || -1.0 % Fraction
@ cell @ cell [ age < true ^ all_nan <= true ] :: distance >- Min
@ cell : missing_prop >> Sum || 128
@ cell @ gene :: frac || 65535
@ cell @ gene :: sparse_umis || 'a b'
@ gene [ marker > 1.0 | ! missing_prop != 5. ] : is_lateral >> Count || .5 Int32
@ type [ missing_prop != nan ] : missing_prop % Round digits 2 type UInt8 >> Count type Int16 || .5
@ cell @ gene :: frac >- Quantile
@ cell @ gene :: sparse_umis % Fraction type int8 || nan UInt32
@ cell @ gene [ all_zero != 2147483647 ] :: frac % Round digits 3
@ cell @ gene :: sparse_umis >- Median
@ batch : missing_prop / donor >> Sum type UInt64
@ cell [ ! with_nan != Inf ] : with_nan >> Min
@ cell : neg_age >> Mode
@ cell : batch % Significant high 2147483648 || 1e10 Bool
@ cell : all_nan % Clamp low 1e-10 >> Median || Infinity int32
@ batch [ ! missing_prop = -Inf ] : missing_prop % Round type Bool || 128 float32
@ type [ ! color > 1 | ! missing_prop ~ 65535 ] : color % Round type Symbol >> Count type Float32
@ cell @ cell :: distance % Clamp high e
@ cell [ ! all_nan != 1e-10 | score = 4294967295 ] : type / neg_age =@
@ gene : is_lateral % Clamp low 1e308 high -1.0
@ cell : batch >> Min || 0x10
@ cell @ cell :: distance >- Mean
@ gene : is_lateral % Round digits 1 type Bool / marker =@ >> Min || 4294967296
@ cell @ gene [ age !~ .5 & score >= 1.5 ] :: UMIs % Round digits 3 type Int32 >| GeoMean eps NaN || 5. Char
@ cell [ i8_age > -32768 ] : ties_int / label =@ || 1e308
@ cell @ gene [ neg_age <= 1.5 ] :: UMIs || 0b10
. title || 'hello world'
@ cell @ cell :: distance % Abs >> Mode
@ cell [ i8_age ~ 1e308 | missing_prop != 4294967295 | i8_age >= 0.0 ] : all_nan % Abs
@ gene : is_lateral >> Quantile
@ cell : is_doublet / all_zero =@
@ gene : is_lateral % Abs type int8 / marker =@ || 'hello world'
. version || 'true'
@ cell [ infs = 1 ] : i8_age / infs
@ cell : all_zero % Convert >> Count || 5.
@ gene [ ! missing_prop < 1_000 ] : missing_prop % Fraction type Char / is_lateral >> Var
@ cell : is_low / ties_int
@ cell : type % Convert type String
@ cell [ ! missing_prop > Infinity | missing_prop !~ 10 & all_neg != 127 ] : batch
. flag || 10 float32
@ gene : missing_prop / is_lateral =@ >> Var
@ cell : label >> GeoMean
@ cell @ gene [ is_doublet >= true ] :: sparse_umis || -1
. intver || 10 int8
@ cell @ gene [ i8_age ~ 128 ] :: sparse_umis % Log base 10 eps 0.5
@ type : missing_prop % Fraction type Char / color || 100
@ cell @ gene :: frac % Round type Int8 >- Quantile
@ batch : missing_prop % Clamp low nan high e
@ cell : ties_int % Significant high pi low 1.5 / batch >> Sum type Float32
@ cell [ type >= U ] : i8_age % Fraction || 255
@ batch [ donor !~ -1.0 ] : missing_prop
@ cell : age >> Sum type UInt16
@ cell @ cell :: distance % Abs type Bool
@ batch : donor % Convert type NotAType
@ batch : missing_prop / donor >> Mean
@ cell [ i8_age ~ Inf & u32_count ~ 32767 ] : u32_count % Convert type Float64
. intver || pi Char
@ cell [ subtype < a b | all_neg <= 65535 & all_nan > NaN ] : all_zero % Fraction / is_doublet || ''
@ type [ missing_prop != 256 ] : color
@ cell @ gene :: sparse_umis % Abs type Char
@ cell [ missing_prop > inf & type <= W ^ all_neg = -1.5 ] : missing_prop / age
@ type : missing_prop % Clamp low 1.5 % Fraction type Bool >> Max
@ cell @ gene :: sparse_umis % Convert >> VarN type Int16
@ gene [ missing_prop = Infinity ^ is_lateral ~ 4294967295 ^ marker >= nan ] : marker % Convert type float32 / marker =@
@ batch [ missing_prop = Infinity & ! missing_prop != 4294967296 & donor >= 0.0 ] : donor % Convert type Bool >> Min
. flag || 'pi'
@ cell [ all_nan ~ 128 ] : neg_age >> Min || 127 Char
@ type [ color < inf | color = 10 ] : missing_prop >> Count type Symbol || 'NaN'
@ gene [ marker <= 100 ] : marker % Round digits 1 type Int64
@ cell [ all_zero <= 1.5 ] : i8_age / label =@
@ type [ color > 0.0 ] : missing_prop >> GeoMean eps 255 || 4294967296
@ cell : all_neg % Round digits 2 type UInt64 >> Sum || 'true'
@ gene : missing_prop || 128 UInt64
@ cell : all_neg || 4294967296
@ gene [ is_lateral > 2147483647 ] : missing_prop % Fraction type int32 % Abs type Int64 / is_lateral
@ cell : neg_age % Fraction type UInt16 || e UInt32
@ gene [ marker < 255 ] : marker >> Min || 1e-10 Float64
@ cell : all_zero
@ cell [ ! is_low <= 5. ^ missing_prop != -Inf ] : with_nan % Abs type Int16 / all_neg =@
@ cell @ gene :: frac % Round
@ cell @ gene :: sparse_umis >- VarN eps pi
@ cell [ ties_int !~ 5. ] : i16_age % Convert type float64 % Fraction / u32_count >> Median
@ cell @ gene [ batch != V ] :: frac || 100
@ cell @ gene :: UMIs % Clamp low 1_000 high nan >- Mode
@ cell @ gene :: frac % Clamp high -32768 >> VarN || 1.0
@ type [ color < inf ] : missing_prop % Log eps 65535 % Convert type UInt32 / color =@ >> StdN type NotAType || -1
@ gene : is_lateral >> VarN type Int16 eps pi
@ cell @ gene :: sparse_umis % Abs type UInt16 || 1_000 UInt64
@ cell @ gene :: sparse_umis >| Sum || -1.5 UInt8
@ cell @ gene [ subtype != U ] :: sparse_umis >> Count type Int16

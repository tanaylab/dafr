% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_log.R
\name{fast_log}
\alias{fast_log}
\title{Parallel \code{log(x + eps) / log(base)} on dense numeric input.}
\usage{
fast_log(
  x,
  eps = 0,
  base = exp(1),
  threshold = dafr_opt("dafr.kernel_threshold")
)
}
\arguments{
\item{x}{Dense numeric matrix or numeric vector. Integers are
silently promoted to double.}

\item{eps}{Non-negative scalar added inside the log. Default 0.}

\item{base}{Positive scalar. Default \code{exp(1)}.}

\item{threshold}{Parallel-for threshold: enable OpenMP only when
\code{length(x) >= threshold}. Defaults to
\code{dafr_opt("dafr.kernel_threshold")}.}
}
\value{
A numeric matrix or vector matching the shape of \code{x}. Names
and dimnames are preserved.
}
\description{
Mirrors the fast-path that \verb{\% Log} takes inside the query evaluator,
but callable directly on an in-memory numeric matrix or vector without
wrapping the input in a \code{\link[=memory_daf]{memory_daf()}}. Intended for downstream
packages whose hot paths already have the matrix materialised in R
(e.g. a session-level cache) and want to avoid the single-threaded
\code{log(x + eps, base)} the query fallback would otherwise incur.
}
\details{
\code{base} values of 2, 10, and \code{exp(1)} dispatch to \code{std::log2},
\code{std::log10}, and \code{std::log} respectively — matching R's preference
for the more accurate form over \code{log(x) / log(base)}. Result is
equal to \code{log(x + eps, base)} to machine precision (verified by the
query-julia-compat parity suite).
}
\examples{
v <- c(1, 10, 100, 1000)
fast_log(v, base = 10)

m <- matrix(runif(12, 0.1, 5), nrow = 3, ncol = 4)
fast_log(m, eps = 1e-5, base = 2)

}
\seealso{
\code{\link[=top_k_per_col]{top_k_per_col()}} for a similarly scoped OpenMP primitive.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open_daf.R
\name{open_daf}
\alias{open_daf}
\title{Open a Daf store by URI or path.}
\usage{
open_daf(uri = NULL, mode = "r", name = NULL, ...)
}
\arguments{
\item{uri}{Path or URL. \verb{memory://} (or \code{NULL} / empty string) for
an in-memory store; a filesystem directory path for \code{files_daf};
a URL with a recognized scheme for future backends.}

\item{mode}{One of \code{"r"}, \code{"r+"}, \code{"w"}, \code{"w+"}. Required for
\code{files_daf}; ignored for \code{memory_daf}.}

\item{name}{Optional name for the daf object. Default derived from
the URI.}

\item{...}{Reserved for backend-specific options.}
}
\value{
A \code{DafReader} or \code{DafWriter} (subclass depends on backend
and mode).
}
\description{
Path/URL-aware factory that dispatches to the right backend.
Supported URIs:
\itemize{
\item \verb{memory://} (or no path / \code{NULL}) — in-memory \code{\link[=memory_daf]{memory_daf()}}.
\item filesystem directory path — \code{\link[=files_daf]{files_daf()}}.
\item \verb{*.daf.zarr} or \verb{*.daf.zarr.zip} (filesystem or HTTP) — \code{\link[=zarr_daf]{zarr_daf()}}.
\item any other \verb{http(s)://} URL — \code{\link[=http_daf]{http_daf()}} (read-only HTTP-served
FilesDaf).
}
}
\details{
HTTP backends are read-only; modes other than \code{"r"} are rejected.
HTTP zip-archive URLs are not supported (open the underlying
\code{.daf.zarr} directory instead).
}
\examples{
d <- open_daf("memory://", name = "demo")
add_axis(d, "cell", c("c1", "c2"))
}

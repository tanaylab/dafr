% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copies.R
\name{copy_matrix}
\alias{copy_matrix}
\title{Copy a matrix from one daf to another.}
\usage{
copy_matrix(
  destination,
  source,
  rows_axis,
  columns_axis,
  name,
  rows_reaxis = NULL,
  columns_reaxis = NULL,
  rename = NULL,
  type = NULL,
  default = .DAFR_UNDEF,
  empty = NULL,
  relayout = TRUE,
  overwrite = FALSE,
  insist = TRUE
)
}
\arguments{
\item{destination}{A \code{DafWriter}.}

\item{source}{A \code{DafReader}.}

\item{rows_axis, columns_axis}{Axis names in \code{source}.}

\item{name}{Matrix name in \code{source}.}

\item{rows_reaxis, columns_reaxis}{If non-NULL, store on these
destination axes (axes must already exist in \code{destination}).}

\item{rename}{If non-NULL, store under this name.}

\item{type}{If non-NULL, coerce to this storage type string.}

\item{default}{If unspecified, missing source raises. If \code{NULL}, silently
skips. Else scalar filled into full source-shape matrix.}

\item{empty}{Value filled for entries whose row or column is missing in
source but present in destination.}

\item{relayout}{If \code{TRUE} (default), also write the transposed layout.}

\item{overwrite, insist}{See \code{\link[=copy_scalar]{copy_scalar()}}.}
}
\value{
Invisibly, the destination.
}
\description{
Mirrors Julia \verb{copy_matrix!(; destination, source, rows_axis, columns_axis, name, rows_reaxis, columns_reaxis, rename, eltype, default, empty, relayout, overwrite, insist)}.
}
\examples{
src <- memory_daf(name = "src")
add_axis(src, "cell", c("c1", "c2"))
add_axis(src, "gene", c("g1", "g2"))
set_matrix(src, "cell", "gene", "UMIs",
           matrix(1:4, nrow = 2,
                  dimnames = list(c("c1","c2"), c("g1","g2"))))
dest <- memory_daf(name = "dest")
add_axis(dest, "cell", c("c1", "c2"))
add_axis(dest, "gene", c("g1", "g2"))
copy_matrix(dest, src, "cell", "gene", "UMIs", relayout = FALSE)
}

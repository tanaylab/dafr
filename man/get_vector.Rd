% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readers.R
\name{get_vector}
\alias{get_vector}
\title{Get a vector, returning it as an axis-named R vector.}
\usage{
get_vector(daf, axis, name, default)
}
\arguments{
\item{daf}{A \code{DafReader}.}

\item{axis}{Axis name.}

\item{name}{Vector name.}

\item{default}{If supplied and the vector is absent, return a named
vector of length \code{axis_length(daf, axis)} with the axis entries as
names. A length-1 \code{default} is recycled to every entry; a length-N
\code{default} (matching the axis length) is used as-is. Any other
length is an error. The vector's atomic type follows \code{default}
(e.g. \code{default = NA} yields \code{logical}, \code{default = "x"} yields
\code{character}, \code{default = 0.0} yields \code{double}).}
}
\value{
Named atomic vector.
}
\description{
Get a vector, returning it as an axis-named R vector.
}
\examples{
# Mirrors readers.jl jldoctest at line 633.
get_vector(example_metacells_daf(), "type", "color")
# memory-B="steelblue" MEBEMP-E="#eebb6e" MEBEMP-L="plum" MPP="gold"

# Default for a missing vector (recycled to axis length):
head(get_vector(example_cells_daf(), "cell", "missing_vec",
                default = NA_character_))
}

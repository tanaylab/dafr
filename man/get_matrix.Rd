% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readers.R
\name{get_matrix}
\alias{get_matrix}
\title{Get a matrix, returning it with axis-entry dimnames.}
\usage{
get_matrix(daf, rows_axis, columns_axis, name, default)
}
\arguments{
\item{daf}{A \code{DafReader}.}

\item{rows_axis}{Row-axis name.}

\item{columns_axis}{Column-axis name.}

\item{name}{Matrix name.}

\item{default}{If supplied and the matrix is absent under both
layouts, return a constant-valued \verb{nrow x ncol} matrix with axis
entries as dimnames.}
}
\value{
Dense \code{matrix} or sparse \code{dgCMatrix} / \code{lgCMatrix} with
dimnames set.
}
\description{
When the matrix is stored only at the flipped-layout axis pair
\verb{(columns_axis, rows_axis)}, this function transposes on-the-fly and
returns with the requested dimnames.
}
\examples{
# Mirrors readers.jl jldoctest at line 933.
m <- get_matrix(example_metacells_daf(), "gene", "metacell", "fraction")
dim(m)            # 683  7
colnames(m)       # 7 metacell IDs
m[1:3, 1:2]
}

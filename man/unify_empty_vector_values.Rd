% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconstruction.R
\name{unify_empty_vector_values}
\alias{unify_empty_vector_values}
\title{Spell "there is no value here" one way in a vector property.}
\usage{
unify_empty_vector_values(
  daf,
  axis,
  property,
  empty_values,
  dtype = NULL,
  empty_value = NULL
)
}
\arguments{
\item{daf}{A \code{DafWriter}.}

\item{axis}{Axis holding the property.}

\item{property}{Name of the vector property to rewrite.}

\item{empty_values}{Value, or vector of values, meaning "there is no
value here". Pass \code{NULL} (or an empty vector) when only converting.}

\item{dtype}{Optional Julia type name (\code{"String"}, \code{"Float32"},
\code{"UInt32"}, ...) to convert the non-empty values to.}

\item{empty_value}{Optional value to store for the empty entries,
overriding the per-type default.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Replace every one of the \code{empty_values} of a \code{property} of an \code{axis}
with a single \code{empty_value}, converting the property to a \code{dtype} on
the way if one is given.
}
\details{
Data arrives spelling absence several ways, often several ways in the
same property: an empty string in some entries and \code{NA} in others,
\code{(Missing)} elsewhere, and for numbers a sentinel such as the smallest
integer, which is a number rather than a visible absence.

This matters before \code{\link[=reconstruct_axis]{reconstruct_axis()}} and \code{\link[=connect_axes]{connect_axes()}}, which
decide what to do with an entry by asking whether its value is empty.

Numbers often arrive as text for exactly this reason - a column of
measurements is a column of strings because a few of its entries say
\code{NA}. Giving a \code{dtype} converts the values that are not empty, which
is an error unless all of them are values of that type; the ones that
are empty become the \code{empty_value}.

By default the \code{empty_value} is the empty string for strings, \code{NaN}
for floats, and \code{0} for unsigned integers (Daf indices are 1-based, so
\code{0} is free to mean "none"). A signed integer or a Boolean has no such
value, so one must be given, or a \code{dtype} that has one.

A property none of whose values is empty is left as it is. What \emph{is}
an error is asking for nothing at all - no \code{empty_values} and no
\code{dtype} - since that cannot do anything whatever the data says.

Unlike Julia's \verb{unify_empty_vector_values!}, the result is not
\code{bestify}d; a mostly-empty result is stored as given.
}
\examples{
d <- memory_daf(name = "d")
add_axis(d, "cell", c("c1", "c2", "c3", "c4"))
set_vector(d, "cell", "batch", c("X", "NA", "(Missing)", ""))
unify_empty_vector_values(d,
    axis = "cell", property = "batch",
    empty_values = c("NA", "(Missing)")
)
get_vector(d, "cell", "batch")
}

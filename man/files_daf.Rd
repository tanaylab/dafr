% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files_daf.R
\name{files_daf}
\alias{files_daf}
\title{File-backed Daf store.}
\usage{
files_daf(path, mode = c("r", "r+", "w", "w+"), name = NULL, packed = FALSE)
}
\arguments{
\item{path}{Directory path.}

\item{mode}{One of \code{"r"} (read-only, store must exist), \code{"r+"}
(read-write, store must exist), \code{"w"} (create; fails if store already
exists), \code{"w+"} (create or open an existing store; if the store
already exists, its \code{scalars}, \code{axes}, \code{vectors}, \code{matrices}
subdirectories and \code{daf.json} are truncated, but unrelated files in
the directory are preserved).}

\item{name}{Human-readable identifier. Defaults to \code{basename(path)}.}

\item{packed}{When \code{TRUE} (writeable modes only), dense vectors, dense
matrices, and large sparse components that exceed the packing threshold are
written as compressed dual-format \code{.zip} shards (DataAxesFormats.jl 0.3.0
"indexed+zipped" packed format) instead of flat binary payloads. Small
components and string properties always stay flat. Tuning is read from the
\code{dafr.packed_compression}, \code{dafr.packed_compression_level}, and
\code{dafr.packed_target_chunk_kb} options. Defaults to \code{FALSE} (flat layout).}
}
\value{
A \code{FilesDaf} instance (\code{DafWriter} under \code{"r+"}/\code{"w"}/\code{"w+"},
\code{FilesDafReadOnly}/\code{DafReadOnly} under \code{"r"}).
}
\description{
A \code{Daf} store backed by a directory of small self-describing files,
bidirectionally compatible with Julia's \code{DataAxesFormats.FilesDaf}.
Writes are non-atomic; only one writer may touch a store at a time.
}
\section{Concurrent access}{

\code{files_daf} does not lock the store. Two writers opening the same
path in mode \code{"r+"} or \code{"w+"} will race on \code{metadata.zip} rebuilds
and per-entry JSON writes, with no guarantee of last-writer-wins
consistency. The supported pattern is single-writer plus arbitrary
read-only readers; cross-process concurrency must be coordinated
externally (e.g., a job scheduler).
}

\examples{
path <- tempfile("dafr-example-")
d <- files_daf(path, mode = "w")
add_axis(d, "cell", c("c1", "c2"))
set_scalar(d, "organism", "human")
rm(d)
unlink(path, recursive = TRUE)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zip_daf.R
\name{zip_daf}
\alias{zip_daf}
\title{Single-file (zip) Daf store.}
\usage{
zip_daf(path, mode = c("r", "r+", "w", "w+"), name = NULL, packed = FALSE)
}
\arguments{
\item{path}{Path to a \code{.daf.zip} archive.}

\item{mode}{One of \code{"r"} (read; must exist), \code{"r+"} (append; must exist),
\code{"w"} (create; fails if it is already a daf archive), \code{"w+"} (create or
append).}

\item{name}{Human-readable identifier. Default derived from the archive's
\code{name} scalar if present, else \code{basename(path)}.}

\item{packed}{When \code{TRUE} (writeable modes), large numeric components are
written as packed \code{.zip} shards, as in \code{\link[=files_daf]{files_daf()}}.}
}
\value{
A \code{ZipDaf} (writable modes) or \code{ZipDafReadOnly} (\code{"r"}).
}
\description{
A \code{Daf} store held in one append-only \code{.daf.zip} archive, byte-compatible
with Julia's \code{DataAxesFormats.ZipDaf}. Same on-disk layout as \code{\link[=files_daf]{files_daf()}}
but inside a ZIP archive (whose central directory replaces \code{metadata.json}).
The archive is append-only: overwriting or deleting a property, or reordering
an axis, raises an error.
}
\examples{
# ZipDaf is built on the POSIX-only MmapZipStore, so it is unavailable on
# Windows; guard the example accordingly.
if (.Platform$OS.type != "windows") {
  path <- tempfile("dafr-zip-", fileext = ".daf.zip")
  d <- zip_daf(path, mode = "w")
  add_axis(d, "cell", c("c1", "c2"))
  set_scalar(d, "organism", "human")
  rm(d)
  unlink(path)
}
}

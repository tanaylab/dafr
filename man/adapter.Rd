% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters.R
\name{adapter}
\alias{adapter}
\title{Apply a computation to a renaming view of a DafWriter.}
\usage{
adapter(
  daf,
  fn,
  input_axes = NULL,
  input_data = NULL,
  output_axes = NULL,
  output_data = NULL,
  capture = memory_daf,
  empty = NULL,
  relayout = TRUE,
  overwrite = FALSE,
  name = ".adapter"
)
}
\arguments{
\item{daf}{A \code{DafWriter} — the base data to read from and write into.}

\item{fn}{A function taking a single \code{DafWriter} argument (the \code{adapted}
chain). Return value passes through.}

\item{input_axes, input_data}{Passed through to \code{\link[=viewer]{viewer()}} for the input
view. At least one of these or \code{output_axes} / \code{output_data} must be
non-NULL (otherwise \code{adapter()} degenerates to \code{fn(daf)}).}

\item{output_axes, output_data}{Passed through to \code{\link[=viewer]{viewer()}} for the
output view.}

\item{capture}{Factory function returning a fresh \code{DafWriter}. Default
\code{memory_daf}.}

\item{empty}{Named list \code{list("<axis>|<vector>" = default, "<r>|<c>|<m>" = default)} supplying default values for entries present in \code{daf}'s axis
but absent from the source view's. NULL (default) disables the feature.}

\item{relayout}{If \code{TRUE} (default), matrix copies also write the
transposed layout.}

\item{overwrite}{If \code{TRUE}, pre-existing destination entries are replaced.}

\item{name}{Human-readable name for the input/capture/adapted dafs. Default
\code{".adapter"}.}
}
\value{
The return value of \code{fn(adapted)}.
}
\description{
Mirrors Julia \verb{adapter(computation, daf; input_axes, input_data, capture, output_axes, output_data, empty, relayout, overwrite, name)}. The typical
use is to run a \verb{@computation} (R: \code{computation()}-wrapped) function whose
expected property names differ from the names stored in \code{daf}.
}
\details{
Flow:
\enumerate{
\item \code{input = viewer(daf, axes = input_axes, data = input_data)} exposes the
subset the computation consumes, possibly under renamed axes / names.
\item \code{capture = capture_factory(name = "<base>.capture")} is a fresh writable.
\item \code{adapted = chain_writer(list(input, capture))} — reads fall through to
\code{input}, writes go to \code{capture}.
\item \code{result = fn(adapted)} — the computation's return value.
\item \code{output = viewer(adapted, axes = output_axes, data = output_data)} —
selects + renames the outputs.
\item Copy \code{output} into \code{daf} via an internal helper. Honors \code{overwrite},
\code{relayout}, and \code{empty}.
\item Return \code{result}.
}
}
\examples{
d <- memory_daf(name = "base")
add_axis(d, "cell", c("c1", "c2", "c3"))
set_vector(d, "cell", "donor", c("d1", "d2", "d1"))
adapter(d,
    function(adapted) {
        entries <- axis_vector(adapted, "obs")
        set_vector(adapted, "obs", "rank", seq_along(entries))
    },
    input_axes  = list(list("obs", "@ cell"), list("cell", NULL)),
    input_data  = VIEW_ALL_VECTORS,
    output_axes = list(list("cell", "@ obs"), list("obs", NULL)),
    output_data = list(list(ALL_VECTORS, NULL), list(c("cell", "rank"), "="))
)
get_vector(d, "cell", "rank")
}
\seealso{
\code{\link[=viewer]{viewer()}}, \code{\link[=chain_writer]{chain_writer()}}, \code{\link[=computation]{computation()}}.
}

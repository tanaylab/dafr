% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threads.R
\name{set_num_threads}
\alias{set_num_threads}
\alias{get_num_threads}
\title{Control OpenMP thread count for dafr kernels.}
\usage{
set_num_threads(n)

get_num_threads()
}
\arguments{
\item{n}{Positive integer; the maximum number of OpenMP threads
to use. Values < 1 are clamped to 1.}
}
\value{
\code{set_num_threads} invisibly returns \code{n}. \code{get_num_threads}
returns the current OpenMP max-threads value.
}
\description{
\code{set_num_threads(n)} caps the number of OpenMP threads used by
every subsequent kernel dispatch. \code{get_num_threads()} reports the
current cap.
}
\details{
dafr's kernels default to the system's OpenMP maximum (typically
\code{OMP_NUM_THREADS} or the machine's CPU count). CRAN policy allows
at most two cores for package checks, and \code{.onLoad()} detects
CRAN-like environments (via \verb{_R_CHECK_LIMIT_CORES_} or an
\code{OMP_THREAD_LIMIT} ≤ 2) and caps to 2 automatically. Interactive
users can raise or lower the cap at any time.
}
\examples{
old <- get_num_threads()
set_num_threads(1L)   # single-thread mode
get_num_threads()     # 1
set_num_threads(old)  # restore
}

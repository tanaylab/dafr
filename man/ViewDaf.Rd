% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_daf.R
\name{ViewDaf}
\alias{ViewDaf}
\title{A read-only view over a base daf.}
\usage{
ViewDaf(
  name = character(0),
  internal = new.env(parent = emptyenv()),
  cache = new.env(parent = emptyenv()),
  axis_version_counter = new.env(parent = emptyenv()),
  vector_version_counter = new.env(parent = emptyenv()),
  matrix_version_counter = new.env(parent = emptyenv()),
  base = DafReader(),
  view_axes = list(),
  view_axis_renames = list(),
  view_axis_indices = list(),
  view_scalars = list(),
  view_vectors = list(),
  view_matrices = list()
)
}
\arguments{
\item{name}{Character scalar; name of the view.}

\item{internal}{Internal environment (created by \code{new_internal_env()}).}

\item{cache}{Cache environment (created by \code{new_cache_env()}).}

\item{axis_version_counter}{Counter environment for axis version tracking.}

\item{vector_version_counter}{Counter environment for vector version tracking.}

\item{matrix_version_counter}{Counter environment for matrix version tracking.}

\item{base}{Base \code{DafReader} this view wraps.}

\item{view_axes}{Named list mapping view axis names to query strings.}

\item{view_axis_renames}{Named list mapping view axis names to base axis
names.}

\item{view_axis_indices}{Named list mapping view axis names to 1-based integer
vectors of positions within each base axis that the view exposes.}

\item{view_scalars}{Named list mapping view scalar names to query strings.}

\item{view_vectors}{Named list mapping \code{"axis|name"} keys to override specs.}

\item{view_matrices}{Named list mapping \code{"rows|cols|name"} keys to override
specs.}
}
\description{
A ViewDaf carries a reference to a base \code{DafReader} and a dictionary of
per-name query overrides (axes / scalars / vectors / matrices). Reads on
the view rewrite into queries against the base; no data is copied.
}
\details{
Users should construct \code{ViewDaf} objects via \code{\link[=viewer]{viewer()}} rather than calling
the constructor directly.
}
\examples{
d <- example_cells_daf()
v <- viewer(d)
inherits(v, "dafr::ViewDaf")
}
\seealso{
\code{\link[=viewer]{viewer()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/http_format.R
\name{HttpDaf}
\alias{HttpDaf}
\alias{http_daf}
\title{Read-only HTTP-served FilesDaf.}
\usage{
HttpDaf(
  name = character(0),
  internal = new.env(parent = emptyenv()),
  cache = new.env(parent = emptyenv()),
  axis_version_counter = new.env(parent = emptyenv()),
  vector_version_counter = new.env(parent = emptyenv()),
  matrix_version_counter = new.env(parent = emptyenv())
)

http_daf(url, name = NULL)
}
\arguments{
\item{name}{Optional override; defaults to the daf's \code{name} scalar (if
any) or the URL basename.}

\item{internal}{Internal per-store environment used by format
backends to stash backend-specific state; reserved for package use.}

\item{cache}{Three-tier cache environment (mapped / memory / query).
See \code{new_cache_env()}.}

\item{axis_version_counter}{Environment tracking per-axis mutation
counters; invalidates cached reads when an axis is modified.}

\item{vector_version_counter}{Environment tracking per-vector
mutation counters.}

\item{matrix_version_counter}{Environment tracking per-matrix
mutation counters.}

\item{url}{HTTP(S) URL pointing at a FilesDaf root.}
}
\value{
A \code{HttpDaf} instance (\code{DafReadOnly} subclass).
}
\description{
A \code{Daf} reader that fetches a \code{\link[=files_daf]{files_daf()}} directory served over
HTTP(S). The server must expose the FilesDaf tree verbatim and include a
\code{metadata.zip} bundle at the root (see \code{\link[=pack_files_daf_metadata]{pack_files_daf_metadata()}}).
From dafr 0.2.0 onward, FilesDaf writes maintain \code{metadata.zip}
automatically; for pre-0.2.0 stores call \code{pack_files_daf_metadata()}
before publishing.
}
\details{
The client downloads \code{metadata.zip} once at open time, parses it in
memory, and serves all JSON metadata from it (no further HTTP traffic
for \verb{format_has_*} / \verb{format_*_set} / scalar reads). Non-JSON payloads
(axis \code{.txt} files, vector/matrix \code{.data} / \code{.nzind} / \code{.nzval} /
\code{.colptr} / \code{.rowval} / \code{.nztxt}) are fetched lazily on first access via
one HTTP GET each, cached by the standard cache layer.

Read-only — mutations are not supported. Server data is assumed stable
while a \link{HttpDaf} is open; reopen to pick up changes.
}
\section{HTTP timeout}{

Each GET uses a 30-second timeout. Override via
\code{options(dafr.http_timeout = N)} (seconds) or environment variable
\code{DAFR_HTTP_TIMEOUT} for slow links or large lazy payloads. Requests
are issued without retry; flaky-network handling is on the caller.
}

\section{URL-derived name}{

When neither \code{name} nor a \code{name} scalar in the daf is set, the
default name is \code{basename(url)}. Two HttpDafs opened from
different hosts but the same basename will collide on name; pass an
explicit \code{name} to disambiguate.
}

\examples{
\dontrun{
d <- http_daf("https://example.com/path/to/data.daf/")
get_scalar(d, "organism")
}
}

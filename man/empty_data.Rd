% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copies.R
\name{empty_data}
\alias{empty_data}
\title{Build a flat-keyed \code{empty} (or \code{types}) list for \code{copy_all()}.}
\usage{
empty_data(
  vectors = list(),
  matrices = list(),
  tensors = list(),
  scalars = list()
)
}
\arguments{
\item{vectors}{List of \code{list(axis, name, value/type)} records.}

\item{matrices}{List of \code{list(rows_axis, columns_axis, name, value/type)}.}

\item{tensors}{List of \code{list(main_axis, rows_axis, columns_axis, name, value/type)}.}

\item{scalars}{List of \code{list(name, value/type)} (typically used with
\code{types}; scalars have no notion of \code{empty}).}
}
\value{
A named list with flat string keys.
}
\description{
Users can pass a plain named list in the flat-key form directly. This
helper assembles one from a more typed builder API. Use \code{value = ...} for
\code{empty} specs, or \code{type = ...} for \code{types} specs.
}
\examples{
empty_data(
    vectors  = list(list(axis = "cell", name = "age", value = 0L)),
    matrices = list(list(rows_axis = "cell", columns_axis = "gene",
                         name = "UMIs", value = 0))
)
}

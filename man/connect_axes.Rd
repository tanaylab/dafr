% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconstruction.R
\name{connect_axes}
\alias{connect_axes}
\title{Record which entry of one axis each entry of another belongs to.}
\usage{
connect_axes(
  daf,
  base_axis,
  from_axis,
  to_axis,
  from_property = NULL,
  to_property = NULL,
  connect_property = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{daf}{A \code{DafWriter}.}

\item{base_axis}{Axis holding both reference properties.}

\item{from_axis}{Axis to create the new property on.}

\item{to_axis}{Axis the new property references.}

\item{from_property}{Property of \code{base_axis} referencing \code{from_axis}
(default: \code{from_axis}).}

\item{to_property}{Property of \code{base_axis} referencing \code{to_axis}
(default: \code{to_axis}).}

\item{connect_property}{Name of the created property of \code{from_axis}
(default: \code{to_axis}).}

\item{overwrite}{Whether to overwrite an existing \code{connect_property}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Given a \code{base_axis} with two vector properties, one holding a
reference to \code{from_axis} and one to \code{to_axis}, create a property of
\code{from_axis} that references \code{to_axis}. This is only possible if every
entry of \code{from_axis} is always associated with a single entry of
\code{to_axis}.
}
\details{
This can happen when one axis (say, "batch") references two other axes
(say, "plate" and "run"). If every batch had a plate and every plate a
run, \code{\link[=reconstruct_axis]{reconstruct_axis()}} would be enough and batch simply wouldn't
have a "run" property. If, however, some batches have a run reference
but no plate reference, we still want to record that "each plate is in
a run" while not giving up on "each batch is in a run", so the data is
duplicated: unlike \code{\link[=reconstruct_axis]{reconstruct_axis()}}, which \emph{moves} data, this
\emph{copies} it and leaves the original in place.

By default the properties of \code{base_axis} holding the references are
named after the axes they refer to, and the created \code{connect_property}
of \code{from_axis} is named after \code{to_axis}. Specify \code{from_property},
\code{to_property} and \code{connect_property} when they are not; a base axis
may refer to the same axis twice (a "sorted_by" and a "sequenced_by"
run, say), in which case the property name is the only thing telling
them apart.

An entry of \code{base_axis} with no \code{from_axis} reference is skipped, so
its \code{to_axis} reference is not examined at all. An entry of
\code{from_axis} that no entry of \code{base_axis} refers to is given an empty
value.
}
\examples{
d <- memory_daf(name = "d")
add_axis(d, "batch", c("B1", "B2", "B3", "B4"))
add_axis(d, "plate", c("P1", "P2", "P3"))
add_axis(d, "run", c("R1", "R2"))
set_vector(d, "batch", "plate", c("P1", "P1", "P2", ""))
set_vector(d, "batch", "run", c("R1", "R1", "R2", "R2"))
connect_axes(d, base_axis = "batch", from_axis = "plate", to_axis = "run")
get_vector(d, "plate", "run") # "R1" "R2" "" -- P3 is named by no batch
}

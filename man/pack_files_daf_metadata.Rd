% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files_metadata_zip.R
\name{pack_files_daf_metadata}
\alias{pack_files_daf_metadata}
\title{Pack a FilesDaf directory's JSON metadata into \code{metadata.zip}.}
\usage{
pack_files_daf_metadata(path)
}
\arguments{
\item{path}{Directory path to a FilesDaf root.}
}
\value{
The absolute path to the written \code{metadata.zip}, invisibly.
}
\description{
Walks \code{path} and bundles \code{daf.json}, \code{axes/metadata.json}, every
\verb{scalars/*.json}, every \verb{vectors/<axis>/*.json}, and every
\verb{matrices/<rows>/<cols>/*.json} into a single \code{path/metadata.zip} archive,
written atomically via \code{metadata.zip.new} + rename. Required for serving a
FilesDaf over HTTP via \code{\link[=http_daf]{http_daf()}}; from dafr 0.2.0 onward, FilesDaf
writes maintain the bundle automatically — call this only to repack a
tree that was built by an older dafr (pre-0.2.0) or modified outside
dafr.
}
\examples{
# The underlying mmap-zip writer is POSIX-only.
if (.Platform$OS.type != "windows") {
  p <- tempfile("daf-")
  files_daf(p, "w+")
  pack_files_daf_metadata(p)
}
}

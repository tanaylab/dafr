% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computations.R
\name{computation}
\alias{computation}
\title{Wrap a function so a contract is enforced on every call.}
\usage{
computation(name, contract, fn)
}
\arguments{
\item{name}{Human-readable computation name (character scalar). Used in
contract violation messages and as a suffix on the wrapper's daf name.}

\item{contract}{A \code{Contract()} describing the inputs and outputs.}

\item{fn}{The function to wrap. Must accept a \code{DafReader}/\code{DafWriter} as
its first argument.}
}
\value{
A function with the same signature as \code{fn}. The bound \code{contract}
and \code{name} are attached as attributes (\code{dafr_contract}, \code{dafr_computation_name}).
}
\description{
Returns a closure that, on each call, wraps its first (\code{daf}) argument in
a \code{contractor()}, runs \code{verify_input()}, executes \code{fn}, then runs
\code{verify_output()}, and returns \code{fn}'s result. When contract enforcement is
disabled (env \code{DAF_ENFORCE_CONTRACTS} / option \code{dafr.enforce_contracts}
are both falsy), the wrapping is still performed but verification is a
no-op, so the wrapper is cheap to leave in place.
}
\details{
Scope: single contract (matching Julia's exported \verb{@computation Contract}
form). Two-contract / three-contract variants (Julia UNTESTED) are not
supported in this slice; pass a single \code{Contract} or use a merged one via
\code{merge_contracts}.
}
\examples{
withr::with_options(list(dafr.enforce_contracts = TRUE), {
    c <- Contract(
        axes = list(cell = list(RequiredInput, "per-cell axis")),
        data = list(contract_vector("cell", "donor",
            RequiredInput, "character", "donor id"))
    )
    read_donors <- computation("read_donors", c,
        function(daf) get_vector(daf, "cell", "donor"))
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("d1", "d2"))
    read_donors(d)
})
}
\seealso{
\code{\link[=function_contract]{function_contract()}}, \code{\link[=contract_description]{contract_description()}}, \code{\link[=contractor]{contractor()}}.
}

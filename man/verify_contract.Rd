% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract_ux.R
\name{verify_contract}
\alias{verify_contract}
\title{Single-pass contract verification (input + output).}
\usage{
verify_contract(contract, daf)
}
\arguments{
\item{contract}{A \code{\link[=Contract]{Contract()}}.}

\item{daf}{A \link{DafReader}.}
}
\value{
Invisibly \code{daf}.
}
\description{
Wraps \code{daf} in a fresh \code{\link[=contractor]{contractor()}} call using \code{contract}, then
runs \code{\link[=verify_input]{verify_input()}} and \code{\link[=verify_output]{verify_output()}}. Errors early with a
diagnostic on contract violation; returns \code{invisible(daf)} on
success.
}
\examples{
withr::with_options(list(dafr.enforce_contracts = TRUE), {
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("A", "B"))
    c <- create_contract(
        axes = list(axis_contract("cell", RequiredInput, "per-cell axis")),
        vectors = list(contract_vector("cell", "donor", RequiredInput,
            "character", "donor id"))
    )
    verify_contract(c, d)
})
}
\seealso{
\code{\link[=create_contract]{create_contract()}}, \code{\link[=verify_input]{verify_input()}}, \code{\link[=verify_output]{verify_output()}},
\code{\link[=contractor]{contractor()}}
}

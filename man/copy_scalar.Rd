% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copies.R
\name{copy_scalar}
\alias{copy_scalar}
\title{Copy a scalar from one daf to another.}
\usage{
copy_scalar(
  destination,
  source,
  name,
  rename = NULL,
  type = NULL,
  default = .DAFR_UNDEF,
  overwrite = FALSE,
  insist = TRUE
)
}
\arguments{
\item{destination}{A \code{DafWriter}.}

\item{source}{A \code{DafReader}.}

\item{name}{Name of the scalar in \code{source}.}

\item{rename}{If non-NULL, store under this name in \code{destination}.}

\item{type}{If non-NULL, coerce to this R storage type string
(\code{"logical"}, \code{"integer"}, \code{"double"}, \code{"numeric"}, \code{"character"}).}

\item{default}{If unspecified, missing source raises. If \code{NULL}, missing
source silently skips. Else, the value is used when source is absent.}

\item{overwrite}{If \code{TRUE}, replace an existing destination scalar.}

\item{insist}{If \code{TRUE} (default) and the destination already has the
scalar, raise; if \code{FALSE}, silently skip.}
}
\value{
Invisibly, the destination.
}
\description{
Mirrors Julia \verb{copy_scalar!(; destination, source, name, rename, type, default, overwrite, insist)}.
}
\examples{
src <- memory_daf(name = "src")
dest <- memory_daf(name = "dest")
set_scalar(src, "organism", "human")
copy_scalar(dest, src, "organism", rename = "species")
get_scalar(dest, "species")
}

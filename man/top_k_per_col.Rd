% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/top_k_per_col.R
\name{top_k_per_col}
\alias{top_k_per_col}
\title{Per-column bounded top-K with OpenMP.}
\usage{
top_k_per_col(
  x,
  k,
  use_abs = FALSE,
  threshold = dafr_opt("dafr.kernel_threshold")
)
}
\arguments{
\item{x}{Numeric dense matrix, or \code{Matrix::dgCMatrix}.}

\item{k}{Positive integer - number of entries to return per column.}

\item{use_abs}{Rank by \code{abs(value)} when \code{TRUE}, else by raw value.
Values returned are always signed.}

\item{threshold}{Parallel-for threshold: enable OpenMP across columns
only when \code{ncol(x) >= threshold}. Defaults to
\code{dafr_opt("dafr.kernel_threshold")}.}
}
\value{
A list with two \code{k}-by-\code{ncol} matrices:
\describe{
\item{\code{indices}}{Integer matrix of row indices (1-based). \code{NA_integer_}
in padding slots.}
\item{\code{values}}{Double matrix of signed values. \code{NA_real_} in padding
slots.}
}
Column names of \code{x} are preserved on the output columns; row-name
lookup is the caller's responsibility (use \code{rownames(x)[indices]}).
}
\description{
Returns the \code{k} largest (or largest-by-magnitude if \code{use_abs = TRUE})
values of each column together with their row indices. Input may be a
dense numeric matrix or a \code{Matrix::dgCMatrix}; the sparse path scans
only the nonzero entries per column so it scales with \code{nnz}, not
\code{nrow * ncol}.
}
\details{
Rows missing a valid comparison score (\code{NA} / \code{NaN} in dense input;
absent from the column's nonzero entries in the CSC input) are
excluded from consideration. When fewer than \code{k} entries qualify, the
tail of each output column is NA-padded.
}
\examples{
m <- matrix(c(1, -5, 3, 2, 4, -2, 7, -1, 6, 5, 8, -3), nrow = 4, ncol = 3)
top_k_per_col(m, k = 2)
top_k_per_col(m, k = 2, use_abs = TRUE)

sm <- Matrix::sparseMatrix(
    i = c(1, 3, 2, 4, 1, 3),
    j = c(1, 1, 2, 2, 3, 3),
    x = c(5, 2, 7, 1, 4, 8),
    dims = c(4, 3)
)
top_k_per_col(sm, k = 2)

}

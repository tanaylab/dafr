% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dplyr_backend.R
\name{compute}
\alias{compute}
\title{Persist \code{mutate()}-produced columns as daf vectors.}
\usage{
compute(x, vectors = NULL, overwrite = FALSE)
}
\arguments{
\item{x}{A \code{daf_axis_tbl}.}

\item{vectors}{Character vector of override column names to persist.
Required — passing \code{NULL} errors, by design, to avoid silent
whole-tbl writes.}

\item{overwrite}{Passed to \code{\link[=set_vector]{set_vector()}}. Defaults to \code{FALSE}.}
}
\value{
Invisibly, \code{x}.
}
\description{
\code{compute()} is the explicit write-back step for a \code{daf_axis_tbl}.
It accepts only columns that currently exist in the tbl's
overrides (i.e. introduced by \code{\link[dplyr:mutate]{dplyr::mutate()}}) and writes each as
a vector on the tbl's axis via \code{\link[=set_vector]{set_vector()}}.
}
\details{
Write-back requires \strong{full row coverage} of the axis. If the tbl
has been \code{\link[dplyr:filter]{dplyr::filter()}}ed (partial row mask), \code{compute()} errors
rather than silently persist partial data. A permuted-but-full row
mask (e.g. after \code{\link[dplyr:arrange]{dplyr::arrange()}}) is un-permuted to axis order
before writing.
}
\examples{
d <- memory_daf()
add_axis(d, "cell", c("c1", "c2", "c3"))
set_vector(d, "cell", "n", c(10, 20, 30))
if (requireNamespace("dplyr", quietly = TRUE)) {
    t <- dplyr::tbl(d, "cell") |> dplyr::mutate(log_n = log10(n))
    compute(t, vectors = "log_n")
    has_vector(d, "cell", "log_n")  # TRUE
}
}

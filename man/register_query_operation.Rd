% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{register_query_operation}
\alias{register_query_operation}
\title{Register a query operation (eltwise or reduction).}
\usage{
register_query_operation(kind, name, fn, overwrite = FALSE, source = NULL)
}
\arguments{
\item{kind}{Either \code{"eltwise"} or \code{"reduction"}.}

\item{name}{Op name; must match \verb{[A-Z][A-Za-z0-9_]*} to be a valid
query token.}

\item{fn}{Function \verb{function(x, ...)} where \code{x} is a numeric value
(scalar / vector / matrix) and \code{...} collects named parameters.}

\item{overwrite}{Logical; replace an already-registered op of the
same kind and name.}

\item{source}{Optional \code{"file:line"} string identifying where the
registration happened. Used in the conflict error message;
auto-captured from the caller's srcref if omitted.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Single entry point for adding custom eltwise / reduction operations
to the dafr query DSL at runtime. Mirrors Julia's
\code{register_query_operation()} from \code{DataAxesFormats.Registry}.
}
\examples{
register_query_operation("eltwise", "Clamp01",
  function(x, ...) pmin(pmax(x, 0), 1),
  overwrite = TRUE)
}

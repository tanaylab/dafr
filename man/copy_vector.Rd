% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copies.R
\name{copy_vector}
\alias{copy_vector}
\title{Copy a vector from one daf to another.}
\usage{
copy_vector(
  destination,
  source,
  axis,
  name,
  rename = NULL,
  reaxis = NULL,
  type = NULL,
  default = .DAFR_UNDEF,
  empty = NULL,
  overwrite = FALSE,
  insist = TRUE
)
}
\arguments{
\item{destination}{A \code{DafWriter}.}

\item{source}{A \code{DafReader}.}

\item{axis}{Axis name in \code{source}.}

\item{name}{Vector name in \code{source}.}

\item{rename}{If non-NULL, store under this name in \code{destination}.}

\item{reaxis}{If non-NULL, store on this (already-existing) destination axis.}

\item{type}{If non-NULL, coerce to this storage type string.}

\item{default}{If unspecified, missing source raises. If \code{NULL}, missing
source silently skips. Else, a scalar (filled into every entry) or
vector used when source is absent.}

\item{empty}{Value filled for destination-axis entries not present in the
source axis (required when source axis is a subset of destination).}

\item{overwrite, insist}{See \code{\link[=copy_scalar]{copy_scalar()}}.}
}
\value{
Invisibly, the destination.
}
\description{
Mirrors Julia \verb{copy_vector!(; destination, source, axis, name, reaxis, rename, eltype, default, empty, overwrite, insist)}.
}
\examples{
src <- memory_daf(name = "src")
add_axis(src, "cell", c("c1", "c2"))
set_vector(src, "cell", "age", c(10L, 20L))
dest <- memory_daf(name = "dest")
add_axis(dest, "cell", c("c1", "c2"))
copy_vector(dest, src, "cell", "age")
}

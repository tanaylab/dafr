% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete.R
\name{base_daf}
\alias{base_daf}
\title{One base repository of a \code{\link[=complete_chain]{complete_chain()}}.}
\usage{
base_daf(daf, axes = NULL, data = NULL)
}
\arguments{
\item{daf}{A \code{DafReader} on disk.}

\item{axes, data}{Optional \code{\link[=viewer]{viewer()}} axes / data spec.}
}
\value{
A \code{dafr_base_daf} spec.
}
\description{
Pairs a \code{DafReader} with the \code{\link[=viewer]{viewer()}} parameters to apply to it,
which restrict it to a subset of its data and/or rename that data.
Pass a plain \code{DafReader} to \code{\link[=complete_chain]{complete_chain()}} instead wherever the
whole of it is used, which is the common case.
}
\examples{
tmp <- tempfile()
dir.create(tmp)
d <- files_daf(tmp, name = "d", mode = "w+")
spec <- base_daf(d, axes = list(list("cell", "=")))
class(spec)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groups.R
\name{compact_groups}
\alias{compact_groups}
\title{Renumber group indices to be dense in 1..N.}
\usage{
compact_groups(group_indices)
}
\arguments{
\item{group_indices}{Integer vector (or coercible).}
}
\value{
\code{list(n_groups = integer(1), group_indices = integer(N))}.
}
\description{
Given a vector where element \code{i} is the group index for entry \code{i}
(0 denotes "no group"), return a list with \code{n_groups} (the number
of unique non-zero groups) and \code{group_indices} (the renumbered
indices, using first-seen order). Zeros are preserved.
}
\examples{
compact_groups(c(5L, 10L, 5L, 0L, 10L))
# $n_groups = 2; $group_indices = c(1, 2, 1, 0, 2)
}
\seealso{
\code{\link[=collect_group_members]{collect_group_members()}}, \code{\link[=group_names]{group_names()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h5df.R
\name{h5df}
\alias{h5df}
\title{Single-file (HDF5) Daf store.}
\usage{
h5df(path, mode = c("r", "r+", "w", "w+"), name = NULL)
}
\arguments{
\item{path}{Path to a \code{.h5df} file.}

\item{mode}{One of \code{"r"} (read; must exist), \code{"r+"} (append; must exist),
\code{"w"} (create; fails if it is already a daf store), \code{"w+"} (create or
append).}

\item{name}{Human-readable identifier. Default derived from the store's
\code{name} scalar if present, else \code{basename(path)}.}
}
\value{
An \code{H5df} (writable modes) or \code{H5dfReadOnly} (\code{"r"}).
}
\description{
A \code{Daf} store held in one \code{.h5df} HDF5 file, interoperable with Julia's
\code{DataAxesFormats.H5df}. The file holds a \code{daf} marker dataset plus
\code{scalars}/\code{axes}/\code{vectors}/\code{matrices} groups of typed HDF5 datasets.
Requires the \code{hdf5r} package.
}
\examples{
if (requireNamespace("hdf5r", quietly = TRUE)) {
  path <- tempfile("dafr-", fileext = ".h5df")
  d <- h5df(path, mode = "w")
  add_axis(d, "cell", c("c1", "c2"))
  set_scalar(d, "organism", "human")
  rm(d)
  unlink(path)
}
}

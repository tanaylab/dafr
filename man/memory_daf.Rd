% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_daf.R
\name{memory_daf}
\alias{memory_daf}
\title{In-memory Daf store.}
\usage{
memory_daf(name = "memory")
}
\arguments{
\item{name}{Human-readable identifier. Defaults to \code{"memory"}.}
}
\value{
A \code{MemoryDaf} instance.
}
\description{
A concrete \code{DafWriter} backed entirely by R environments — no disk,
no mmap. Scalars, axes, vectors, and matrices live in nested
environments (hash tables) under the \code{internal} property:
}
\details{
\itemize{
\item \code{internal$scalars}     : \code{env(name -> value)}
\item \code{internal$axes}        : \code{env(axis -> list(entries = character, dict = env))}
\item \code{internal$vectors}     : \code{env(axis -> env(name -> vector))}
\item \code{internal$matrices}    : \code{env(rows_axis -> env(cols_axis -> env(name -> matrix)))}
}
}
\examples{
d <- memory_daf(name = "scratch")
add_axis(d, "cell", c("A", "B", "C"))
set_vector(d, "cell", "donor", c("d1", "d2", "d1"))
}

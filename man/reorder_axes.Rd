% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reorder.R
\name{reorder_axes}
\alias{reorder_axes}
\title{Reorder one or more axis entries in place.}
\usage{
reorder_axes(daf, ..., crash_counter = NULL)
}
\arguments{
\item{daf}{A \link{DafWriter}.}

\item{...}{Named permutations: \verb{cell = perm_cell, gene = perm_gene}.}

\item{crash_counter}{Internal — for testing only. See
\code{tick_crash_counter()} (not exported).}
}
\value{
Invisibly the input \code{daf}.
}
\description{
Permutes the entries of one or more axes in the given daf, rewriting
every vector and matrix that depends on those axes. On \code{files_daf}
the operation is crash-recoverable via a \verb{.reorder.backup/}
directory of hardlinks; on the next open with mode \code{"r+"} or \code{"w+"},
any in-progress reorder is automatically rolled back to the
pre-reorder state.
}
\details{
Each \code{...} argument is a name = integer-permutation pair, where the
permutation is a 1-based vector of length \code{axis_length(daf, name)}.
After reorder, the entry at position \code{i} is the entry that was at
position \code{permutation[i]} before — i.e. \code{new_entries[i] = old_entries[permutation[i]]}.
}
\examples{
d <- memory_daf()
add_axis(d, "cell", c("A", "B", "C"))
set_vector(d, "cell", "x", c(10, 20, 30))
reorder_axes(d, cell = c(3L, 1L, 2L))
axis_vector(d, "cell")
get_vector(d, "cell", "x")
}

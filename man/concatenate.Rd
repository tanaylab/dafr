% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat.R
\name{concatenate}
\alias{concatenate}
\title{Concatenate multiple dafs along one or more axes.}
\usage{
concatenate(
  destination,
  axis,
  sources,
  names = NULL,
  dataset_axis = "dataset",
  dataset_property = TRUE,
  prefix = FALSE,
  prefixed = NULL,
  empty = NULL,
  merge = NULL,
  sparse_if_saves_storage_fraction = 0.25,
  overwrite = FALSE
)
}
\arguments{
\item{destination}{A \code{DafWriter}. Must be empty of the concatenation axes.}

\item{axis}{A single axis name or a character vector of axis names.}

\item{sources}{List of \code{DafReader}s to concatenate.}

\item{names}{Optional character vector of unique data set names (defaults
to each source's \code{name} prop).}

\item{dataset_axis}{Name of the per-source axis to create. \code{NULL} disables.}

\item{dataset_property}{If \code{TRUE} (default) and \code{dataset_axis} is non-NULL,
create a same-named vector on every concatenation axis holding the
source name for each entry.}

\item{prefix}{Logical (single or per-axis). Prefix concat-axis entries
with \code{"<dataset_name>."} to de-duplicate across sources.}

\item{prefixed}{Optional character vector (or list of vectors per axis)
of additional property names to prefix, beyond the heuristic (same-name
or \code{"<axis>.*"} properties).}

\item{empty}{Named list of fill values for missing per-source properties.}

\item{merge}{Named list mapping property keys to a merge action
(\code{"SkipProperty"}, \code{"LastValue"}, \code{"CollectAxis"}).}

\item{sparse_if_saves_storage_fraction}{Numeric (default 0.25); reserved
for a future sparse-promotion heuristic.}

\item{overwrite}{If \code{TRUE}, allow replacing pre-existing destination
entries.}
}
\value{
Invisibly, the destination.
}
\description{
Mirrors Julia \verb{concatenate!()}. For each concatenation axis, entries from
each source are appended in source order. Non-concat axes must be
identical across all sources and are copied once.
}
\examples{
a <- memory_daf(name = "A"); add_axis(a, "cell", c("a1", "a2"))
b <- memory_daf(name = "B"); add_axis(b, "cell", c("b1"))
dest <- memory_daf(name = "dest")
concatenate(dest, "cell", list(a, b))
}

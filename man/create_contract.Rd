% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract_ux.R
\name{create_contract}
\alias{create_contract}
\title{Construct a \code{\link[=Contract]{Contract()}} from typed per-category argument lists.}
\usage{
create_contract(
  scalars = list(),
  vectors = list(),
  matrices = list(),
  tensors = list(),
  axes = list(),
  is_relaxed = FALSE
)
}
\arguments{
\item{scalars}{List of \code{\link[=contract_scalar]{contract_scalar()}} records.}

\item{vectors}{List of \code{\link[=contract_vector]{contract_vector()}} records.}

\item{matrices}{List of \code{\link[=contract_matrix]{contract_matrix()}} records.}

\item{tensors}{List of \code{\link[=tensor_contract]{tensor_contract()}} records.}

\item{axes}{List of \code{\link[=axis_contract]{axis_contract()}} records.}

\item{is_relaxed}{Logical; if \code{TRUE}, accesses to properties
outside the contract are allowed at enforcement time.}
}
\value{
A \code{\link[=Contract]{Contract()}} object.
}
\description{
User-friendly constructor that concatenates \code{scalars}, \code{vectors},
\code{matrices}, and \code{tensors} into the flat \code{data} slot of \code{\link[=Contract]{Contract()}},
and converts \code{axes} (a list of \code{\link[=axis_contract]{axis_contract()}} records) into the
named-list form the underlying class expects.
}
\examples{
create_contract(
    axes = list(
        axis_contract("cell", RequiredInput, "per-cell axis"),
        axis_contract("gene", RequiredInput, "per-gene axis")
    ),
    scalars = list(contract_scalar("organism", RequiredInput, "character", "species")),
    vectors = list(contract_vector("cell", "donor", RequiredInput, "character", "donor id")),
    matrices = list(contract_matrix("cell", "gene", "UMIs", RequiredInput, "integer", "UMIs"))
)
}
\seealso{
\code{\link[=Contract]{Contract()}}, \code{\link[=axis_contract]{axis_contract()}}, \code{\link[=tensor_contract]{tensor_contract()}},
\code{\link[=verify_contract]{verify_contract()}}, \code{\link[=contract_docs]{contract_docs()}}
}

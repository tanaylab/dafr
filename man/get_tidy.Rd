% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataframes.R
\name{get_tidy}
\alias{get_tidy}
\title{Pivot axis vectors into a tidy long-format tibble.}
\usage{
get_tidy(daf, axis, columns = NULL, cache = TRUE, ...)
}
\arguments{
\item{daf}{A \link{DafReader}.}

\item{axis}{Axis name.}

\item{columns}{Optional character vector of vector names to
include. Defaults to all vectors on \code{axis}.}

\item{cache}{Logical; if \code{TRUE} (default), the result is memoised
in the query cache and served on repeat calls.}

\item{...}{Passed to \code{\link[tidyr:pivot_longer]{tidyr::pivot_longer()}}.}
}
\value{
A \code{tibble} with columns \code{name}, \code{key}, \code{value}.
}
\description{
Requires \code{tidyr} and \code{tibble}; errors with an install hint if
either is missing.
}
\examples{
if (requireNamespace("tidyr", quietly = TRUE) &&
    requireNamespace("tibble", quietly = TRUE)) {
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("A", "B"))
    get_tidy(d, "cell")
}
}
\seealso{
\code{\link[=get_dataframe]{get_dataframe()}}, \code{\link[=get_dataframe_query]{get_dataframe_query()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anndata_format.R
\name{h5ad_as_daf}
\alias{h5ad_as_daf}
\title{Load a Muon-style h5ad file into a \code{memory_daf}.}
\usage{
h5ad_as_daf(path, name = NULL, mode = "r", unsupported_handler = WARN_HANDLER)
}
\arguments{
\item{path}{Path to the \code{.h5ad} file.}

\item{name}{Optional name for the returned Daf; defaults to the
file basename without extension.}

\item{mode}{\code{hdf5r} open mode. Default \code{"r"} (read-only).}

\item{unsupported_handler}{Handler for h5ad features we don't
translate (default \link[=handler-constants]{WARN_HANDLER}). Accepts
\code{ERROR_HANDLER}, \code{WARN_HANDLER}, \code{IGNORE_HANDLER}, or a
\verb{function(message)} dispatched via \code{\link[=inefficient_action_handler]{inefficient_action_handler()}}.}
}
\value{
A \code{\link[=memory_daf]{memory_daf()}} populated with the h5ad contents.
}
\description{
Loads dense or sparse \verb{/X}, \verb{/obs} and \verb{/var} column groups (dense
scalar columns and categorical columns), flat + nested \verb{/uns} scalar
entries (nested keys are flattened via \verb{_}), and dense \verb{/layers}.
\code{varm} / \code{obsm} / \code{obsp} / \code{varp} and \code{raw} are still escalated
through \code{unsupported_handler} and skipped.
}
\details{
Requires the \code{hdf5r} and \code{Matrix} packages.
}
\examples{
\dontrun{
p <- system.file("extdata", "small_test.h5ad", package = "dafr")
d <- h5ad_as_daf(p)
is_daf(d)
}
}
\seealso{
\code{\link[=daf_as_h5ad]{daf_as_h5ad()}}, \link{DafAnnData}, \code{\link[=as_anndata]{as_anndata()}}
}

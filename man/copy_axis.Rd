% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copies.R
\name{copy_axis}
\alias{copy_axis}
\title{Copy an axis (its entries) from one daf to another.}
\usage{
copy_axis(
  destination,
  source,
  axis,
  rename = NULL,
  overwrite = FALSE,
  insist = TRUE
)
}
\arguments{
\item{destination}{A \code{DafWriter}.}

\item{source}{A \code{DafReader}.}

\item{axis}{Axis name in \code{source}.}

\item{rename}{If non-NULL, use this name in \code{destination}.}

\item{overwrite}{If \code{TRUE}, delete any existing destination axis (and all
its properties) before recreating.}

\item{insist}{If \code{TRUE} (default) and the destination already has the axis,
raise; if \code{FALSE}, silently skip.}
}
\value{
Invisibly, the destination.
}
\description{
Mirrors Julia \verb{copy_axis!(; destination, source, axis, rename, overwrite, insist)}.
}
\examples{
src <- memory_daf(name = "src"); add_axis(src, "cell", c("c1", "c2"))
dest <- memory_daf(name = "dest")
copy_axis(dest, src, "cell")
}

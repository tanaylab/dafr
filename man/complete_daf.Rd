% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete.R
\name{complete_daf}
\alias{complete_daf}
\title{Reopen a persistent chain from disk.}
\usage{
complete_daf(leaf, mode = "r", name = NULL)
}
\arguments{
\item{leaf}{Filesystem path to the leaf daf.}

\item{mode}{\code{"r"} or \code{"r+"}.}

\item{name}{Optional name.}
}
\value{
A \code{DafReader} or \code{DafWriter}.
}
\description{
Walks the \code{base_daf_repository} scalar chain rooted at \code{leaf}, opening
each level with \code{\link[=open_daf]{open_daf()}}. Returns a \code{chain_reader} (\code{mode = "r"}) or
\code{chain_writer} (\code{mode = "r+"}, only the leaf is writable).
}
\examples{
tmp_root <- tempfile(); dir.create(tmp_root)
base_dir <- file.path(tmp_root, "base")
new_dir <- file.path(tmp_root, "new")
files_daf(base_dir, name = "base", mode = "w+")
new <- files_daf(new_dir, name = "new", mode = "w+")
complete_chain(
    base_daf = open_daf(base_dir, "r"),
    new_daf = new, absolute = TRUE
)
chain <- complete_daf(new_dir, "r")
}

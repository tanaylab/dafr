% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anndata_format.R
\name{daf_as_h5ad}
\alias{daf_as_h5ad}
\title{Write a Daf to a Muon-style h5ad file.}
\usage{
daf_as_h5ad(
  daf,
  path,
  obs_axis = NULL,
  var_axis = NULL,
  x_name = "UMIs",
  overwrite = FALSE,
  unsupported_handler = WARN_HANDLER
)
}
\arguments{
\item{daf}{A \link{DafReader}.}

\item{path}{Destination \code{.h5ad} path.}

\item{obs_axis}{Axis name mapped to \verb{/obs}. If \code{NULL}, auto-detected
from \code{"cell"}, \code{"metacell"}.}

\item{var_axis}{Axis name mapped to \verb{/var}. If \code{NULL}, auto-detected
from \code{"gene"}.}

\item{x_name}{Matrix name (on the \verb{obs_axis, var_axis} pair) written
as \verb{/X}. Default \code{"UMIs"}.}

\item{overwrite}{If \code{FALSE} (default), error when \code{path} already
exists; if \code{TRUE}, silently replace.}

\item{unsupported_handler}{Handler for Daf features we cannot
represent in h5ad. See \code{\link[=inefficient_action_handler]{inefficient_action_handler()}}.}
}
\value{
Invisibly, \code{path}.
}
\description{
Inverse of \code{\link[=h5ad_as_daf]{h5ad_as_daf()}}. Writes \verb{/X} (dense or CSC-sparse),
per-axis column groups \verb{/obs} and \verb{/var} (each with an \verb{_index};
factor vectors emitted as h5ad categorical groups), \verb{/layers} for
additional matrices, and flat \verb{/uns} scalars.
}
\details{
Note: the write side always emits \verb{/uns} flat. On read we flatten
nested uns groups with a \verb{_} separator, so a round-trip of a nested
uns file produces flat dotted keys on the resulting Daf.

Requires the \code{hdf5r} and \code{Matrix} packages.
}
\examples{
\dontrun{
d <- memory_daf()
add_axis(d, "cell", c("c1", "c2"))
add_axis(d, "gene", c("g1", "g2"))
set_matrix(d, "cell", "gene", "UMIs", matrix(1:4, 2, 2))
p <- tempfile(fileext = ".h5ad")
daf_as_h5ad(d, p, obs_axis = "cell", var_axis = "gene")
}
}
\seealso{
\code{\link[=h5ad_as_daf]{h5ad_as_daf()}}, \link{DafAnnData}, \code{\link[=as_anndata]{as_anndata()}}
}

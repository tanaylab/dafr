% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/http_store.R
\name{HttpStore}
\alias{HttpStore}
\title{Read-only HTTP-served Zarr store.}
\usage{
HttpStore(
  url = character(0),
  zmetadata = list(),
  chunk_cache = new.env(parent = emptyenv())
)
}
\arguments{
\item{url}{Root URL of the served Zarr v2 directory.}

\item{zmetadata}{Parsed consolidated \code{.zmetadata} content.}

\item{chunk_cache}{Environment caching fetched chunk bytes by path.}
}
\description{
Implements the \link{ZarrStore} interface over HTTP(S). Used by
\code{\link[=zarr_daf]{zarr_daf()}} when given an \verb{http(s)://} URL pointing at a \code{.daf.zarr}
directory served by a static web server. Reads \code{.zmetadata}
(consolidated metadata) once at open; subsequent \code{store_exists} /
\code{store_list} resolve against the parsed dictionary, and chunk fetches
are cached in process memory.
}
\details{
Writes hard-error: HTTP-served Zarr is read-only.

Zarr version: this backend currently targets the \strong{Zarr v2} on-disk
layout, where discoverability hinges on a consolidated \code{.zmetadata}
index at the store root. It has \strong{not} yet been ported to the Zarr v3
format that the rest of \code{dafr} reads and writes locally. v3 stores do
not write \code{.zmetadata} (v3 inlines the consolidated metadata into the
root \code{zarr.json}), so opening a v3 \code{.daf.zarr} over HTTP via
\code{\link[=zarr_daf]{zarr_daf()}} currently fails with a \verb{.zmetadata not found} error.
Porting \code{HttpStore} to v3 is a separate follow-up; local directory
(\code{DirStore}) and zip (\code{MmapZipStore}) v3 stores are fully supported, as
is \code{http_daf()} (the FilesFormat-over-HTTP path, which does not use
Zarr).
}

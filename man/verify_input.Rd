% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{verify_input}
\alias{verify_input}
\alias{verify_output}
\title{Verify contract inputs / outputs}
\usage{
verify_input(daf)

verify_output(daf)
}
\arguments{
\item{daf}{A \link{DafReader} — if it is a \link{ContractDaf}, the associated
contract is verified; otherwise the call is a silent no-op.}
}
\value{
\code{invisible()}.
}
\description{
\code{verify_input()} should be called before running a computation and
\code{verify_output()} after. Both are no-ops when \code{daf} is not a
\link{ContractDaf} (i.e. enforcement is disabled). \code{verify_output()}
additionally fails on unused \code{RequiredInput} properties.
}
\examples{
withr::with_options(list(dafr.enforce_contracts = TRUE), {
    c <- Contract(
        axes = list(cell = list(RequiredInput, "per-cell axis")),
        data = list(contract_vector("cell", "donor",
            RequiredInput, "character", "donor id"))
    )
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("d1", "d2"))
    cd <- contractor("demo", c, d)
    verify_input(cd)
    get_vector(cd, "cell", "donor")
    verify_output(cd)
})
}

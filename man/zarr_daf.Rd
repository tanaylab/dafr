% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zarr_format.R
\name{zarr_daf}
\alias{zarr_daf}
\title{Open a Zarr-backed Daf store.}
\usage{
zarr_daf(uri = NULL, mode = c("r", "r+", "w", "w+"), name = NULL)
}
\arguments{
\item{uri}{Filesystem directory path, \verb{:memory:}, or \code{NULL}.}

\item{mode}{\code{"r"}, \code{"r+"}, \code{"w"}, or \code{"w+"}. \code{"w"} initializes a
fresh empty store (creating the directory if needed). \code{"w+"}
truncates any existing store and creates a fresh one. \code{"r+"}
opens for read-write without truncating. \code{"r"} opens read-only.}

\item{name}{Optional name; default derived from \code{uri}.}
}
\value{
A \code{ZarrDaf} (writeable) or \code{ZarrDafReadOnly}.
}
\description{
Path-aware constructor that picks the right backing store. A path
ending in \code{.daf.zarr} (or any directory path) creates a \code{DirStore}.
\verb{:memory:} or \code{NULL} creates a \code{DictStore}. \verb{*.daf.zarr.zip} errors
with "lands in slice 17" pending the C++ MmapZipStore.
}
\examples{
tmp <- tempfile(fileext = ".daf.zarr")
d <- zarr_daf(tmp, mode = "w")
add_axis(d, "cell", c("c1", "c2"))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copies.R
\name{copy_all}
\alias{copy_all}
\title{Copy everything from one daf to another.}
\usage{
copy_all(
  destination,
  source,
  empty = NULL,
  types = NULL,
  overwrite = FALSE,
  insist = TRUE,
  relayout = TRUE
)
}
\arguments{
\item{destination}{A \code{DafWriter}.}

\item{source}{A \code{DafReader}.}

\item{empty}{Named list mapping flat keys to fill values:
\code{"axis|name" -> value} for vectors, \code{"rows|cols|name" -> value} for
matrices. Matrix keys accept either axis order.}

\item{types}{Named list of type-coercion strings in the same flat-key form
plus \code{"name"} (no pipes) for scalars.}

\item{overwrite}{If \code{TRUE}, replace pre-existing destination entries.}

\item{insist}{If \code{TRUE} (default) raise on pre-existing conflicts when
\code{overwrite = FALSE}; if \code{FALSE} silently skip.}

\item{relayout}{If \code{TRUE} (default), also write transposed layout for
copied matrices.}
}
\value{
Invisibly, the destination.
}
\description{
Mirrors Julia \verb{copy_all!(; destination, source, empty, types, overwrite, insist, relayout)}. Copies in order: scalars, axes, vectors, matrices.
Tensors are not auto-expanded here — users can call \code{\link[=copy_tensor]{copy_tensor()}}
explicitly for per-main-axis-entry matrix groups.
}
\details{
Axes already in the destination are not overwritten (regardless of the
\code{overwrite} flag). A destination axis must be identical to or a subset of
the source axis (else \code{empty} is required per-vector / per-matrix to fill
missing entries). Unknown-to-source destination axes are left untouched.
}
\examples{
src <- memory_daf(name = "src")
set_scalar(src, "organism", "human")
add_axis(src, "cell", c("c1", "c2"))
set_vector(src, "cell", "age", c(10L, 20L))
dest <- memory_daf(name = "dest")
copy_all(dest, src, relayout = FALSE)
}

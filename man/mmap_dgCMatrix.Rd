% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mmap.R
\name{mmap_dgCMatrix}
\alias{mmap_dgCMatrix}
\title{Construct a \code{dgCMatrix} whose \code{x}, \code{i}, \code{p} slots are ALTREP mmap views.}
\usage{
mmap_dgCMatrix(x_path, i_path, p_path, nrow, ncol, nnz, dimnames = NULL)
}
\arguments{
\item{x_path, i_path, p_path}{Paths to the \code{.bin} files.}

\item{nrow, ncol, nnz}{Matrix shape + number of non-zeros.}

\item{dimnames}{Optional \code{list(rowname_character, colname_character)}.}
}
\description{
The three files must be the CSC component files of a sparse matrix:
\code{x_path} stores \code{nnz} doubles, \code{i_path} stores \code{nnz} int32 row indices
(0-based, matching \code{dgCMatrix@i}), \code{p_path} stores \code{ncol+1} int32
column pointers (0-based, matching \code{dgCMatrix@p}).
}

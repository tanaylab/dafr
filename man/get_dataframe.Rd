% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataframes.R
\name{get_dataframe}
\alias{get_dataframe}
\title{Extract vectors on an axis as a \code{data.frame}.}
\usage{
get_dataframe(daf, axis, columns = NULL, cache = TRUE)
}
\arguments{
\item{daf}{A \link{DafReader}.}

\item{axis}{Axis name.}

\item{columns}{Optional character vector of vector names to
include. Defaults to all vectors on \code{axis}.}

\item{cache}{Logical; if \code{TRUE} (default), the result is memoised
in the query cache and served on repeat calls.}
}
\value{
A \code{data.frame}.
}
\description{
Returns a \code{data.frame} with one column per vector on \code{axis}, rows
named by the axis entries. When \code{columns} is \code{NULL}, all vectors
defined on \code{axis} are included.
}
\examples{
d <- memory_daf()
add_axis(d, "cell", c("c1", "c2"))
set_vector(d, "cell", "donor", c("A", "B"))
get_dataframe(d, "cell")
}
\seealso{
\code{\link[=get_dataframe_query]{get_dataframe_query()}}, \code{\link[=get_tidy]{get_tidy()}}
}

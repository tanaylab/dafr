% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{ContractDaf}
\alias{ContractDaf}
\title{Contract-enforcing daf wrapper}
\usage{
ContractDaf(
  name = character(0),
  internal = new.env(parent = emptyenv()),
  cache = new.env(parent = emptyenv()),
  axis_version_counter = new.env(parent = emptyenv()),
  vector_version_counter = new.env(parent = emptyenv()),
  matrix_version_counter = new.env(parent = emptyenv()),
  computation = character(0),
  is_relaxed = logical(0),
  overwrite = logical(0),
  base = DafReader(),
  axes = new.env(parent = emptyenv()),
  data = new.env(parent = emptyenv())
)
}
\arguments{
\item{name}{Human-readable identifier for the \code{Daf} store.}

\item{internal}{Internal per-store environment used by format
backends to stash backend-specific state; reserved for package use.}

\item{cache}{Three-tier cache environment (mapped / memory / query).
See \code{new_cache_env()}.}

\item{axis_version_counter}{Environment tracking per-axis mutation
counters; invalidates cached reads when an axis is modified.}

\item{vector_version_counter}{Environment tracking per-vector
mutation counters.}

\item{matrix_version_counter}{Environment tracking per-matrix
mutation counters.}

\item{computation}{Name of the computation being guarded.}

\item{is_relaxed}{If \code{TRUE}, accesses to properties outside the
contract are allowed (matching \code{Contract(is_relaxed = TRUE)}).}

\item{overwrite}{If \code{TRUE}, pre-existing \code{CreatedOutput} properties
are allowed.}

\item{base}{Underlying \code{DafReader} / \code{DafWriter}.}

\item{axes}{Per-axis access-tracking environment (keyed by axis
name).}

\item{data}{Per-property access-tracking environment (keyed by
\verb{<kind>:<axes>:<name>}).}
}
\value{
An S7 class object; users normally obtain instances via
\code{\link[=contractor]{contractor()}} rather than calling the constructor directly.
}
\description{
S7 class returned by \code{\link[=contractor]{contractor()}} when enforcement is enabled.
Intercepts \verb{format_*} reads/writes on an underlying \link{DafReader}
to track access and reject operations outside the contract.
}
\seealso{
\code{\link[=contractor]{contractor()}}, \code{\link[=verify_input]{verify_input()}}, \code{\link[=verify_output]{verify_output()}}.
}

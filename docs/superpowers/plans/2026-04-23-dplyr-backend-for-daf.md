# dplyr Backend for daf — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let users manipulate a daf axis with familiar dplyr verbs — `tbl(daf, "cell") |> filter(...) |> mutate(...) |> group_by(...) |> summarise(...)` — and explicitly write back results with `compute()`.

**Architecture:** A new S3 class `daf_axis_tbl` wraps `(daf, axis)` plus pending state: `row_mask` (integer indices into the axis; `NULL` = full), `col_select` (character, or `NULL` = show all stored + all overrides), `overrides` (named list of vectors aligned to `row_mask` order), and `groups` (character vector of grouping vars). Each verb is **eager**: on call it collects the current logical tibble, delegates the hard work to dplyr, then rewraps or returns a plain tibble when a row-identity transition (distinct / summarise / pull) warrants it.

**Key invariant:** `overrides[[nm]]` is always aligned to the current `row_mask`. `arrange()` therefore commits its permutation to `row_mask` (and reorders overrides) rather than using a separate "order" attribute — that keeps the model simple and avoids double-permutation bugs between `mutate` and `arrange`.

DSL pushdown is **explicitly out of scope for v1** — correctness first, perf later. dplyr / tibble / tidyselect are Suggests, and our S3 methods are registered conditionally in `.onLoad` so the package still installs without them.

**Tech stack:** R, S7 (existing), S3 method dispatch (new), `dplyr` (Suggests), `tibble` (Suggests), `tidyselect` (Suggests), `rlang` (already Imports), `testthat` (Suggests).

---

## File Structure

**Files to create**
- `R/dplyr_backend.R` — the `daf_axis_tbl` class, constructor, every verb method, `collect()`, `compute()`, `print()`. One file; the surface is tightly coupled.
- `tests/testthat/test-dplyr-basics.R` — tbl(), collect(), as_tibble(), print().
- `tests/testthat/test-dplyr-verbs.R` — select, pull, filter, mutate, arrange, distinct.
- `tests/testthat/test-dplyr-summarise.R` — summarise ungrouped and grouped, auto-tie-back.
- `tests/testthat/test-dplyr-compute.R` — write-back semantics.
- `vignettes/dplyr.Rmd` — user-facing vignette.

**Files to modify**
- `DESCRIPTION` — add `dplyr`, `tibble`, `tidyselect` to `Suggests`.
- `R/zzz.R` — in `.onLoad`, conditionally register S3 methods against dplyr generics.
- `R/dafr-package.R` (or via roxygen in new file) — no exports from the new file; S3 methods are internal and registered dynamically.
- `_pkgdown.yml` — add a new "dplyr backend" section.
- `NAMESPACE` — regenerated by roxygen; will export `daf_axis_tbl` class helpers and `compute()` if we export them (`compute` is tricky — see Task 11).
- `NEWS.md` — new entry under unreleased.

---

## Prerequisites / Conventions

- Test invocation: `cd tests && Rscript testthat.R`. Many tests use `memory_daf()` + `add_axis()` + `set_vector()` to build fixtures — follow that pattern.
- A known pre-existing failure in `test-helpers.R:26` is expected under `Rscript`; ignore it.
- `NOT_CRAN=true` enables stress tests; not required for this plan.
- After editing `R/*.R`: run `Rscript -e 'devtools::document()'` to regenerate `NAMESPACE` and `man/`, then `R CMD INSTALL .` before running tests (the installed namespace is what `library(dafr)` in tests loads).
- Every task ends with an explicit commit. Commit messages follow the existing `<type>(<scope>): <summary>` style from recent commits (`docs(10d): ...`, `release(10d): ...`). For this feature use scope `dplyr`.

---

## Task 1: Foundation — Suggests, class, constructor, registration

**Files:**
- Modify: `DESCRIPTION`
- Create: `R/dplyr_backend.R`
- Modify: `R/zzz.R`
- Create: `tests/testthat/test-dplyr-basics.R`

- [ ] **Step 1: Add Suggests to DESCRIPTION**

Edit `DESCRIPTION`, add `dplyr`, `tibble`, `tidyselect` to the `Suggests:` block (alphabetical). After edit, the block should contain them alongside the existing `testthat`, `tidyr`, etc. Do not move anything to Imports.

- [ ] **Step 2: Write the failing test**

Create `tests/testthat/test-dplyr-basics.R`:

```r
test_that("tbl() on a Daf returns a daf_axis_tbl for the named axis", {
    skip_if_not_installed("dplyr")
    skip_if_not_installed("tibble")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "donor", c("A", "B", "A"))
    x <- dplyr::tbl(d, "cell")
    expect_s3_class(x, "daf_axis_tbl")
    expect_identical(attr(x, "axis"), "cell")
    expect_true(identical(attr(x, "daf"), d))
})

test_that("tbl() errors for missing axis", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    expect_error(dplyr::tbl(d, "cell"), "axis")
})
```

- [ ] **Step 3: Run test to verify it fails**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-basics|FAIL|fail"
```
Expected: FAIL — `tbl()` has no method for a Daf.

- [ ] **Step 4: Create R/dplyr_backend.R with class and tbl method**

```r
#' @include classes.R queries.R readers.R dataframes.R
NULL

# ---- Internal constructor --------------------------------------------------

new_daf_axis_tbl <- function(daf, axis,
                             row_mask = NULL,
                             col_select = NULL,
                             overrides = list(),
                             groups = character()) {
    if (!is_daf(daf)) stop("`daf` must be a DafReader", call. = FALSE)
    if (!has_axis(daf, axis)) {
        stop(sprintf("axis %s is not in the daf", sQuote(axis)), call. = FALSE)
    }
    structure(
        list(),
        daf = daf,
        axis = axis,
        row_mask = row_mask,
        col_select = col_select,
        overrides = overrides,
        groups = groups,
        class = "daf_axis_tbl"
    )
}

# Invariant: `overrides[[nm]]` is always aligned to `row_mask` order.
# `arrange()` commits its permutation to `row_mask` (and reorders
# overrides accordingly), so there is no separate "order" attribute.

# S7 method on dplyr::tbl for DafReader. Registered lazily; see .onLoad.
tbl.dafr__DafReader <- function(src, axis, ...) {
    new_daf_axis_tbl(src, axis)
}
```

(The odd function name `tbl.dafr__DafReader` is a placeholder chosen so `registerS3method` can bind it to class `"dafr::DafReader"` at load time — S3 dispatch uses the class string, not the function name.)

- [ ] **Step 5: Register S3 methods in .onLoad**

Edit `R/zzz.R`:

```r
.onLoad <- function(libname, pkgname) {
    set_default_options()
    .register_default_ops()
    S7::methods_register()
    .register_dplyr_methods()
    invisible()
}

.register_dplyr_methods <- function() {
    if (!requireNamespace("dplyr", quietly = TRUE)) return(invisible())
    ns <- asNamespace("dafr")
    registerS3method("tbl", "dafr::DafReader", ns$tbl.dafr__DafReader, envir = asNamespace("dplyr"))
}
```

- [ ] **Step 6: Run test to verify it passes**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-basics|FAIL"
```
Expected: PASS for both basics tests.

- [ ] **Step 7: Commit**

```bash
git add DESCRIPTION R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-basics.R NAMESPACE
git commit -m "feat(dplyr): add daf_axis_tbl class and tbl() constructor"
```

---

## Task 2: collect() / as_tibble() / print()

**Files:**
- Modify: `R/dplyr_backend.R`
- Modify: `R/zzz.R` (register new methods)
- Modify: `tests/testthat/test-dplyr-basics.R`

- [ ] **Step 1: Write failing tests**

Append to `tests/testthat/test-dplyr-basics.R`:

```r
test_that("collect() returns a tibble of the axis's vectors", {
    skip_if_not_installed("dplyr")
    skip_if_not_installed("tibble")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "donor", c("A", "B", "A"))
    set_vector(d, "cell", "age", c(10L, 20L, 30L))
    t <- dplyr::collect(dplyr::tbl(d, "cell"))
    expect_s3_class(t, "tbl_df")
    expect_setequal(colnames(t), c("name", "donor", "age"))
    expect_identical(t$name, c("c1", "c2", "c3"))
    expect_identical(t$donor, c("A", "B", "A"))
    expect_identical(t$age, c(10L, 20L, 30L))
})

test_that("as_tibble() is equivalent to collect()", {
    skip_if_not_installed("dplyr")
    skip_if_not_installed("tibble")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("A", "B"))
    t1 <- dplyr::collect(dplyr::tbl(d, "cell"))
    t2 <- tibble::as_tibble(dplyr::tbl(d, "cell"))
    expect_identical(t1, t2)
})

test_that("print() produces informative output without error", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("A", "B"))
    out <- capture.output(print(dplyr::tbl(d, "cell")))
    expect_true(any(grepl("daf_axis_tbl", out)))
    expect_true(any(grepl("cell", out)))
})
```

- [ ] **Step 2: Run tests to verify failure**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-basics|FAIL"
```
Expected: 3 new FAILs (no method for collect/as_tibble/print).

- [ ] **Step 3: Implement collect / as_tibble / print**

Append to `R/dplyr_backend.R`:

```r
# ---- Realization -----------------------------------------------------------

# Apply row_mask, col_select, overrides, groups to produce a tibble.
# All vectors (stored and override) are emitted in row_mask order.
.realize <- function(x) {
    daf <- attr(x, "daf")
    axis <- attr(x, "axis")
    entries <- axis_entries(daf, axis)
    n <- length(entries)

    rm <- attr(x, "row_mask")
    if (is.null(rm)) rm <- seq_len(n)

    stored_cols <- vectors_set(daf, axis)
    sel <- attr(x, "col_select")
    sel_explicit <- !is.null(sel)
    if (!sel_explicit) sel <- stored_cols

    overrides <- attr(x, "overrides")

    cols <- list()
    cols[["name"]] <- entries[rm]
    for (nm in sel) {
        if (nm %in% names(overrides)) {
            cols[[nm]] <- overrides[[nm]]
        } else if (nm %in% stored_cols) {
            v <- get_vector(daf, axis, nm)
            cols[[nm]] <- v[rm]
        }
        # Silently skip names that are neither stored nor overridden —
        # this can happen in an intermediate state but indicates a bug
        # if it survives to collect().
    }
    # If the user didn't explicitly select, surface any extra overrides
    # (e.g. mutations) that weren't in the default selection.
    if (!sel_explicit) {
        extra <- setdiff(names(overrides), c(sel, "name"))
        for (nm in extra) cols[[nm]] <- overrides[[nm]]
    }

    out <- tibble::as_tibble(cols)

    grps <- attr(x, "groups")
    if (length(grps)) out <- dplyr::group_by(out, !!!rlang::syms(grps))

    out
}

collect.daf_axis_tbl <- function(x, ...) .realize(x)

as_tibble.daf_axis_tbl <- function(x, ...) {
    out <- .realize(x)
    if (inherits(out, "grouped_df")) out <- dplyr::ungroup(out)
    out
}

print.daf_axis_tbl <- function(x, n = 6L, ...) {
    daf <- attr(x, "daf")
    axis <- attr(x, "axis")
    rm <- attr(x, "row_mask")
    nrows <- if (is.null(rm)) axis_length(daf, axis) else length(rm)
    cat(sprintf("<daf_axis_tbl>  axis: %s  [%d rows]\n", axis, nrows))
    grps <- attr(x, "groups")
    if (length(grps)) cat("groups:", paste(grps, collapse = ", "), "\n")
    print(utils::head(.realize(x), n))
    invisible(x)
}
```

- [ ] **Step 4: Register new methods in .onLoad**

Edit `.register_dplyr_methods()` in `R/zzz.R`:

```r
.register_dplyr_methods <- function() {
    if (!requireNamespace("dplyr", quietly = TRUE)) return(invisible())
    ns <- asNamespace("dafr")
    registerS3method("tbl", "dafr::DafReader", ns$tbl.dafr__DafReader, envir = asNamespace("dplyr"))
    registerS3method("collect", "daf_axis_tbl", ns$collect.daf_axis_tbl, envir = asNamespace("dplyr"))
    if (requireNamespace("tibble", quietly = TRUE)) {
        registerS3method("as_tibble", "daf_axis_tbl", ns$as_tibble.daf_axis_tbl, envir = asNamespace("tibble"))
    }
    registerS3method("print", "daf_axis_tbl", ns$print.daf_axis_tbl)
}
```

- [ ] **Step 5: Run tests to verify**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-basics|FAIL"
```
Expected: all 5 basics tests PASS.

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-basics.R
git commit -m "feat(dplyr): implement collect/as_tibble/print for daf_axis_tbl"
```

---

## Task 3: select() and pull()

**Files:**
- Modify: `R/dplyr_backend.R`
- Modify: `R/zzz.R`
- Create: `tests/testthat/test-dplyr-verbs.R`

- [ ] **Step 1: Write failing tests**

Create `tests/testthat/test-dplyr-verbs.R`:

```r
test_that("select() trims visible columns; returns daf_axis_tbl", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("A", "B"))
    set_vector(d, "cell", "age", c(1L, 2L))
    out <- dplyr::tbl(d, "cell") |> dplyr::select(donor)
    expect_s3_class(out, "daf_axis_tbl")
    df <- dplyr::collect(out)
    expect_setequal(colnames(df), c("name", "donor"))
})

test_that("pull() returns a vector for a stored column", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(10L, 20L, 30L))
    v <- dplyr::tbl(d, "cell") |> dplyr::pull(age)
    expect_identical(v, c(10L, 20L, 30L))
})

test_that("pull() without column returns the 'name' column", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "age", c(1L, 2L))
    v <- dplyr::tbl(d, "cell") |> dplyr::pull()
    expect_identical(v, c(1L, 2L))  # dplyr default: last column
})
```

- [ ] **Step 2: Run tests to verify failure**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```
Expected: 3 FAILs.

- [ ] **Step 3: Implement select and pull**

Append to `R/dplyr_backend.R`:

```r
select.daf_axis_tbl <- function(.data, ...) {
    daf <- attr(.data, "daf")
    axis <- attr(.data, "axis")
    available <- c(vectors_set(daf, axis), names(attr(.data, "overrides")))
    chosen <- tidyselect::eval_select(rlang::expr(c(...)), setNames(seq_along(available), available))
    attr(.data, "col_select") <- names(chosen)
    .data
}

pull.daf_axis_tbl <- function(.data, var = -1, name = NULL, ...) {
    df <- .realize(.data)
    if (inherits(df, "grouped_df")) df <- dplyr::ungroup(df)
    dplyr::pull(df, {{ var }}, name = {{ name }})
}
```

- [ ] **Step 4: Register methods**

Append inside `.register_dplyr_methods()`:

```r
    registerS3method("select", "daf_axis_tbl", ns$select.daf_axis_tbl, envir = asNamespace("dplyr"))
    registerS3method("pull", "daf_axis_tbl", ns$pull.daf_axis_tbl, envir = asNamespace("dplyr"))
```

- [ ] **Step 5: Run tests to verify**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```
Expected: PASS for all three.

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-verbs.R
git commit -m "feat(dplyr): add select() and pull() verbs"
```

---

## Task 4: filter()

**Files:**
- Modify: `R/dplyr_backend.R`
- Modify: `R/zzz.R`
- Modify: `tests/testthat/test-dplyr-verbs.R`

- [ ] **Step 1: Write failing tests**

Append to `tests/testthat/test-dplyr-verbs.R`:

```r
test_that("filter() reduces rows; result is still a daf_axis_tbl", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(10L, 20L, 30L))
    out <- dplyr::tbl(d, "cell") |> dplyr::filter(age > 15)
    expect_s3_class(out, "daf_axis_tbl")
    df <- dplyr::collect(out)
    expect_identical(df$name, c("c2", "c3"))
    expect_identical(df$age, c(20L, 30L))
})

test_that("filter() composes with select()", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(10L, 20L, 30L))
    set_vector(d, "cell", "donor", c("A", "B", "C"))
    df <- dplyr::tbl(d, "cell") |>
        dplyr::filter(age > 15) |>
        dplyr::select(donor) |>
        dplyr::collect()
    expect_identical(df$name, c("c2", "c3"))
    expect_setequal(colnames(df), c("name", "donor"))
})

test_that("filter() supports multi-variable predicates", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(10L, 20L, 30L))
    set_vector(d, "cell", "donor", c("A", "B", "A"))
    df <- dplyr::tbl(d, "cell") |>
        dplyr::filter(age > 15, donor == "A") |>
        dplyr::collect()
    expect_identical(df$name, "c3")
})
```

- [ ] **Step 2: Verify tests fail**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```

- [ ] **Step 3: Implement filter**

Append to `R/dplyr_backend.R`:

```r
filter.daf_axis_tbl <- function(.data, ..., .by = NULL) {
    df <- .realize(.data)
    if (inherits(df, "grouped_df")) df <- dplyr::ungroup(df)
    keep <- dplyr::filter(df, ...)
    daf <- attr(.data, "daf")
    axis <- attr(.data, "axis")
    # New row_mask: positions in the full axis of the kept entries.
    attr(.data, "row_mask") <- match(keep$name, axis_entries(daf, axis))
    # Reindex overrides (aligned to previous row_mask order) to match
    # the new row_mask order.
    overrides <- attr(.data, "overrides")
    if (length(overrides)) {
        local_idx <- match(keep$name, df$name)
        attr(.data, "overrides") <- lapply(overrides, function(v) v[local_idx])
    }
    .data
}
```

- [ ] **Step 4: Register**

Add inside `.register_dplyr_methods()`:

```r
    registerS3method("filter", "daf_axis_tbl", ns$filter.daf_axis_tbl, envir = asNamespace("dplyr"))
```

- [ ] **Step 5: Verify tests pass**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-verbs.R
git commit -m "feat(dplyr): add filter() verb"
```

---

## Task 5: mutate()

**Files:**
- Modify: `R/dplyr_backend.R`
- Modify: `R/zzz.R`
- Modify: `tests/testthat/test-dplyr-verbs.R`

- [ ] **Step 1: Write failing tests**

Append to `tests/testthat/test-dplyr-verbs.R`:

```r
test_that("mutate() adds a computed column visible to later verbs", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "a", c(1, 2, 3))
    set_vector(d, "cell", "b", c(10, 20, 30))
    df <- dplyr::tbl(d, "cell") |>
        dplyr::mutate(s = a + b) |>
        dplyr::collect()
    expect_identical(df$s, c(11, 22, 33))
})

test_that("mutate() chains — later expressions see earlier ones", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "a", c(1, 2))
    df <- dplyr::tbl(d, "cell") |>
        dplyr::mutate(b = a * 2, c = b + 1) |>
        dplyr::collect()
    expect_identical(df$b, c(2, 4))
    expect_identical(df$c, c(3, 5))
})

test_that("mutate() before filter() survives the filter", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "a", c(1, 2, 3))
    df <- dplyr::tbl(d, "cell") |>
        dplyr::mutate(sq = a * a) |>
        dplyr::filter(sq > 1) |>
        dplyr::collect()
    expect_identical(df$name, c("c2", "c3"))
    expect_identical(df$sq, c(4, 9))
})
```

- [ ] **Step 2: Verify failure**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```

- [ ] **Step 3: Implement mutate**

Append to `R/dplyr_backend.R`:

```r
mutate.daf_axis_tbl <- function(.data, ...) {
    df <- .realize(.data)
    if (inherits(df, "grouped_df")) {
        df2 <- dplyr::mutate(df, ...)
        df2 <- dplyr::ungroup(df2)
    } else {
        df2 <- dplyr::mutate(df, ...)
    }
    # New columns (added by this mutate) and existing override/stored cols
    # that got overwritten go into overrides.
    old_names <- colnames(df)
    new_or_changed <- setdiff(colnames(df2), "name")
    overrides <- attr(.data, "overrides")
    for (nm in new_or_changed) {
        if (!(nm %in% old_names) || !identical(df[[nm]], df2[[nm]])) {
            overrides[[nm]] <- df2[[nm]]
        }
    }
    attr(.data, "overrides") <- overrides
    .data
}
```

- [ ] **Step 4: Register**

Add:

```r
    registerS3method("mutate", "daf_axis_tbl", ns$mutate.daf_axis_tbl, envir = asNamespace("dplyr"))
```

- [ ] **Step 5: Verify tests pass**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-verbs.R
git commit -m "feat(dplyr): add mutate() verb"
```

---

## Task 6: arrange() and distinct()

**Files:**
- Modify: `R/dplyr_backend.R`
- Modify: `R/zzz.R`
- Modify: `tests/testthat/test-dplyr-verbs.R`

- [ ] **Step 1: Write failing tests**

Append:

```r
test_that("arrange() sorts rows by a column", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(30L, 10L, 20L))
    df <- dplyr::tbl(d, "cell") |> dplyr::arrange(age) |> dplyr::collect()
    expect_identical(df$name, c("c2", "c3", "c1"))
    expect_identical(df$age, c(10L, 20L, 30L))
})

test_that("arrange(desc(...)) works", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(30L, 10L, 20L))
    df <- dplyr::tbl(d, "cell") |> dplyr::arrange(dplyr::desc(age)) |> dplyr::collect()
    expect_identical(df$age, c(30L, 20L, 10L))
})

test_that("distinct() exits daf_axis_tbl and returns a tibble", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "donor", c("A", "B", "A"))
    out <- dplyr::tbl(d, "cell") |> dplyr::distinct(donor)
    expect_s3_class(out, "tbl_df")
    expect_false(inherits(out, "daf_axis_tbl"))
    expect_setequal(out$donor, c("A", "B"))
})
```

- [ ] **Step 2: Verify failure**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```

- [ ] **Step 3: Implement arrange and distinct**

Append to `R/dplyr_backend.R`:

```r
arrange.daf_axis_tbl <- function(.data, ..., .by_group = FALSE) {
    df <- .realize(.data)
    if (inherits(df, "grouped_df")) df <- dplyr::ungroup(df)
    sorted <- dplyr::arrange(df, ...)
    local_idx <- match(sorted$name, df$name)
    daf <- attr(.data, "daf")
    axis <- attr(.data, "axis")
    old_rm <- attr(.data, "row_mask")
    if (is.null(old_rm)) old_rm <- seq_len(axis_length(daf, axis))
    attr(.data, "row_mask") <- old_rm[local_idx]
    overrides <- attr(.data, "overrides")
    if (length(overrides)) {
        attr(.data, "overrides") <- lapply(overrides, function(v) v[local_idx])
    }
    .data
}

distinct.daf_axis_tbl <- function(.data, ..., .keep_all = FALSE) {
    df <- .realize(.data)
    if (inherits(df, "grouped_df")) df <- dplyr::ungroup(df)
    dplyr::distinct(df, ..., .keep_all = .keep_all)
}
```

- [ ] **Step 4: Register**

```r
    registerS3method("arrange", "daf_axis_tbl", ns$arrange.daf_axis_tbl, envir = asNamespace("dplyr"))
    registerS3method("distinct", "daf_axis_tbl", ns$distinct.daf_axis_tbl, envir = asNamespace("dplyr"))
```

- [ ] **Step 5: Verify tests pass**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-verbs|FAIL"
```

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-verbs.R
git commit -m "feat(dplyr): add arrange() and distinct() verbs"
```

---

## Task 7: summarise() (ungrouped)

**Files:**
- Modify: `R/dplyr_backend.R`
- Modify: `R/zzz.R`
- Create: `tests/testthat/test-dplyr-summarise.R`

- [ ] **Step 1: Write failing tests**

Create `tests/testthat/test-dplyr-summarise.R`:

```r
test_that("summarise() ungrouped returns a 1-row tibble", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(10, 20, 30))
    out <- dplyr::tbl(d, "cell") |> dplyr::summarise(m = mean(age), n = dplyr::n())
    expect_s3_class(out, "tbl_df")
    expect_false(inherits(out, "daf_axis_tbl"))
    expect_identical(nrow(out), 1L)
    expect_identical(out$m, 20)
    expect_identical(out$n, 3L)
})

test_that("filter() |> summarise() respects the filter", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "age", c(10, 20, 30))
    out <- dplyr::tbl(d, "cell") |>
        dplyr::filter(age > 15) |>
        dplyr::summarise(m = mean(age))
    expect_identical(out$m, 25)
})
```

- [ ] **Step 2: Verify failure**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-summarise|FAIL"
```

- [ ] **Step 3: Implement summarise (ungrouped path only — grouped comes next task)**

Append to `R/dplyr_backend.R`:

```r
summarise.daf_axis_tbl <- function(.data, ..., .by = NULL, .groups = NULL) {
    df <- .realize(.data)
    grps <- attr(.data, "groups")
    if (length(grps) == 0) {
        if (inherits(df, "grouped_df")) df <- dplyr::ungroup(df)
        return(dplyr::summarise(df, ...))
    }
    # Grouped path deferred to Task 8.
    stop("grouped summarise not yet implemented", call. = FALSE)
}
```

- [ ] **Step 4: Register**

```r
    registerS3method("summarise", "daf_axis_tbl", ns$summarise.daf_axis_tbl, envir = asNamespace("dplyr"))
    registerS3method("summarize", "daf_axis_tbl", ns$summarise.daf_axis_tbl, envir = asNamespace("dplyr"))
```

- [ ] **Step 5: Verify tests pass**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-summarise|FAIL"
```

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-summarise.R
git commit -m "feat(dplyr): add ungrouped summarise() verb"
```

---

## Task 8: group_by() + grouped summarise() with auto-tie-back

**Files:**
- Modify: `R/dplyr_backend.R`
- Modify: `R/zzz.R`
- Modify: `tests/testthat/test-dplyr-summarise.R`

- [ ] **Step 1: Write failing tests**

Append to `tests/testthat/test-dplyr-summarise.R`:

```r
test_that("group_by() stores group var on the tbl", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "donor", c("A", "B"))
    g <- dplyr::tbl(d, "cell") |> dplyr::group_by(donor)
    expect_s3_class(g, "daf_axis_tbl")
    expect_identical(attr(g, "groups"), "donor")
})

test_that("grouped summarise with non-axis group var returns a plain tibble", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3", "c4"))
    set_vector(d, "cell", "donor", c("A", "B", "A", "B"))
    set_vector(d, "cell", "age", c(10, 20, 30, 40))
    out <- dplyr::tbl(d, "cell") |>
        dplyr::group_by(donor) |>
        dplyr::summarise(m = mean(age), n = dplyr::n())
    expect_s3_class(out, "tbl_df")
    expect_false(inherits(out, "daf_axis_tbl"))
    expect_setequal(out$donor, c("A", "B"))
    expect_identical(out$m[out$donor == "A"], 20)
    expect_identical(out$n[out$donor == "A"], 2L)
})

test_that("grouped summarise with axis group var ties back to that axis", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "donor", c("A", "B"))
    add_axis(d, "cell", c("c1", "c2", "c3", "c4"))
    set_vector(d, "cell", "donor", c("A", "B", "A", "B"))
    set_vector(d, "cell", "age", c(10, 20, 30, 40))
    out <- dplyr::tbl(d, "cell") |>
        dplyr::group_by(donor) |>
        dplyr::summarise(mean_age = mean(age), n = dplyr::n())
    expect_s3_class(out, "daf_axis_tbl")
    expect_identical(attr(out, "axis"), "donor")
    df <- dplyr::collect(out)
    expect_setequal(df$name, c("A", "B"))
    expect_true("mean_age" %in% colnames(df))
})

test_that("auto-tie-back partial coverage is fine", {
    # donors axis has A,B,C but filtered cells only mention A,B
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "donor", c("A", "B", "C"))
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "donor", c("A", "B", "A"))
    set_vector(d, "cell", "age", c(10, 20, 30))
    out <- dplyr::tbl(d, "cell") |>
        dplyr::group_by(donor) |>
        dplyr::summarise(n = dplyr::n())
    expect_s3_class(out, "daf_axis_tbl")
    expect_identical(attr(out, "axis"), "donor")
    df <- dplyr::collect(out)
    expect_setequal(df$name, c("A", "B"))
})
```

- [ ] **Step 2: Verify failure**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-summarise|FAIL"
```

- [ ] **Step 3: Implement group_by and grouped summarise**

Append to `R/dplyr_backend.R`:

```r
group_by.daf_axis_tbl <- function(.data, ..., .add = FALSE, .drop = dplyr::group_by_drop_default(.data)) {
    vars <- vapply(rlang::enquos(...), rlang::as_name, character(1))
    prev <- attr(.data, "groups")
    attr(.data, "groups") <- if (.add) unique(c(prev, vars)) else vars
    .data
}

ungroup.daf_axis_tbl <- function(x, ...) {
    attr(x, "groups") <- character()
    x
}

# Replace the placeholder summarise from Task 7.
summarise.daf_axis_tbl <- function(.data, ..., .by = NULL, .groups = NULL) {
    df <- .realize(.data)
    grps <- attr(.data, "groups")
    if (length(grps) == 0) {
        if (inherits(df, "grouped_df")) df <- dplyr::ungroup(df)
        return(dplyr::summarise(df, ...))
    }
    result <- dplyr::summarise(df, ..., .groups = "drop")
    daf <- attr(.data, "daf")
    # Auto-tie-back: single group var, that var names an axis in the daf.
    if (length(grps) == 1L && has_axis(daf, grps)) {
        axis_name <- grps
        all_entries <- axis_entries(daf, axis_name)
        group_col <- result[[axis_name]]
        if (is.character(group_col) && all(group_col %in% all_entries)) {
            rm <- match(group_col, all_entries)
            # Derived columns = everything in `result` except the grouping key.
            derived <- as.list(result[, setdiff(colnames(result), axis_name), drop = FALSE])
            out <- new_daf_axis_tbl(
                daf = daf,
                axis = axis_name,
                row_mask = rm,
                col_select = names(derived),  # show derived cols explicitly
                overrides = derived
            )
            return(out)
        }
    }
    result
}
```

- [ ] **Step 4: Register**

```r
    registerS3method("group_by", "daf_axis_tbl", ns$group_by.daf_axis_tbl, envir = asNamespace("dplyr"))
    registerS3method("ungroup", "daf_axis_tbl", ns$ungroup.daf_axis_tbl, envir = asNamespace("dplyr"))
```

- [ ] **Step 5: Verify tests pass**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-summarise|FAIL"
```

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R R/zzz.R tests/testthat/test-dplyr-summarise.R
git commit -m "feat(dplyr): add group_by + grouped summarise with axis tie-back"
```

---

## Task 9: compute() — explicit write-back

**Files:**
- Modify: `R/dplyr_backend.R`
- Create: `tests/testthat/test-dplyr-compute.R`

- [ ] **Step 1: Write failing tests**

Create `tests/testthat/test-dplyr-compute.R`:

```r
test_that("compute() with vectors= writes named overrides back as vectors", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "a", c(1, 2, 3))
    out <- dplyr::tbl(d, "cell") |>
        dplyr::mutate(sq = a * a, double = a * 2)
    compute(out, vectors = "sq")
    expect_true(has_vector(d, "cell", "sq"))
    expect_false(has_vector(d, "cell", "double"))
    expect_identical(get_vector(d, "cell", "sq"), c(1, 4, 9))
})

test_that("compute() errors when row_mask is partial (filtered tbl)", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2", "c3"))
    set_vector(d, "cell", "a", c(1, 2, 3))
    out <- dplyr::tbl(d, "cell") |>
        dplyr::filter(a > 1) |>
        dplyr::mutate(sq = a * a)
    expect_error(compute(out, vectors = "sq"), "partial|filter|row")
})

test_that("compute() errors for names not in overrides", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "a", c(1, 2))
    out <- dplyr::tbl(d, "cell")
    expect_error(compute(out, vectors = "sq"), "sq|override|mutate")
})

test_that("compute() default vectors=NULL errors with a hint", {
    # Explicit design call: never silently write back everything.
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "cell", c("c1", "c2"))
    set_vector(d, "cell", "a", c(1, 2))
    out <- dplyr::tbl(d, "cell") |> dplyr::mutate(sq = a * a)
    expect_error(compute(out), "vectors")
})
```

- [ ] **Step 2: Verify failure**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-compute|FAIL"
```

- [ ] **Step 3: Implement compute**

Append to `R/dplyr_backend.R`:

```r
#' Write named mutate()-produced columns back as daf vectors.
#'
#' `compute()` is the explicit write-back step for a [daf_axis_tbl].
#' It accepts *only* named columns that currently exist in the tbl's
#' overrides (i.e. were introduced by [dplyr::mutate()]), and writes
#' each as a vector on the tbl's axis via [set_vector()].
#'
#' Write-back requires a full row mask — if the tbl has been
#' [dplyr::filter()]ed or otherwise row-subset, `compute()` errors
#' rather than silently write partial data.
#'
#' @param x A `daf_axis_tbl`.
#' @param vectors Character vector of override column names to persist.
#'   Required. Passing `NULL` errors (by design, to avoid silent
#'   whole-tbl writes).
#' @param overwrite Passed to [set_vector()]. Defaults to `FALSE`.
#' @return Invisibly, `x`.
#' @export
compute <- function(x, vectors = NULL, overwrite = FALSE) {
    UseMethod("compute")
}

#' @export
compute.daf_axis_tbl <- function(x, vectors = NULL, overwrite = FALSE) {
    if (is.null(vectors) || !length(vectors)) {
        stop("compute(): `vectors` must name override columns to persist",
             call. = FALSE)
    }
    overrides <- attr(x, "overrides")
    missing <- setdiff(vectors, names(overrides))
    if (length(missing)) {
        stop(sprintf("compute(): not present as an override: %s. Create with mutate().",
                     paste(sQuote(missing), collapse = ", ")),
             call. = FALSE)
    }
    daf <- attr(x, "daf")
    axis <- attr(x, "axis")
    rm <- attr(x, "row_mask")
    n <- axis_length(daf, axis)
    # Permute overrides back to axis order before writing.
    if (is.null(rm)) {
        reorder_idx <- NULL  # already in axis order
    } else if (length(rm) == n && !anyDuplicated(rm)) {
        reorder_idx <- order(rm)  # full coverage in some permuted order
    } else {
        stop("compute(): tbl has a partial row mask (filtered/reduced); cannot write back a full vector. Undo the filter or explicitly extend.",
             call. = FALSE)
    }
    for (nm in vectors) {
        v <- overrides[[nm]]
        if (!is.null(reorder_idx)) v <- v[reorder_idx]
        set_vector(daf, axis, nm, v, overwrite = overwrite)
    }
    invisible(x)
}
```

Note: `compute` is also a dplyr generic in `dbplyr`. We export ours as a regular S3 generic. If this collides with a user-loaded dbplyr/dplyr compute, the dispatch still goes to our method for `daf_axis_tbl`. Verify behavior in this task.

- [ ] **Step 4: Add roxygen export and regenerate**

```bash
Rscript -e 'devtools::document()'
```
Expected: `compute` and `compute.daf_axis_tbl` now appear in `NAMESPACE`.

- [ ] **Step 5: Verify tests pass**

```bash
R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr-compute|FAIL"
```

- [ ] **Step 6: Commit**

```bash
git add R/dplyr_backend.R NAMESPACE man/ tests/testthat/test-dplyr-compute.R
git commit -m "feat(dplyr): add explicit compute() write-back"
```

---

## Task 10: Full-stack integration test

**Files:**
- Modify: `tests/testthat/test-dplyr-summarise.R`

- [ ] **Step 1: Write integration test**

Append to `tests/testthat/test-dplyr-summarise.R`:

```r
test_that("end-to-end example from the plan works", {
    skip_if_not_installed("dplyr")
    d <- memory_daf()
    add_axis(d, "donor", c("A", "B"))
    add_axis(d, "cell", paste0("c", 1:6))
    set_vector(d, "cell", "donor",       c("A", "A", "A", "B", "B", "B"))
    set_vector(d, "cell", "n_umis",      c(500, 1500, 2000, 800, 1200, 3000))
    set_vector(d, "cell", "is.doublet",  c(FALSE, FALSE, TRUE, FALSE, FALSE, FALSE))
    out <- dplyr::tbl(d, "cell") |>
        dplyr::filter(n_umis > 1000, !is.doublet) |>
        dplyr::mutate(log_umis = log10(n_umis)) |>
        dplyr::group_by(donor) |>
        dplyr::summarise(n = dplyr::n(), mean_log = mean(log_umis)) |>
        dplyr::arrange(dplyr::desc(n))
    df <- dplyr::collect(out)
    # Donor A: c2 (umis=1500, not doublet). Donor B: c5 (1200) + c6 (3000).
    expect_setequal(df$name, c("A", "B"))
    expect_identical(df$n[df$name == "A"], 1L)
    expect_identical(df$n[df$name == "B"], 2L)
})
```

- [ ] **Step 2: Run and verify**

```bash
cd tests && Rscript testthat.R 2>&1 | grep -E "dplyr|FAIL"
```
Expected: all dplyr tests PASS, including this integration case.

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-dplyr-summarise.R
git commit -m "test(dplyr): add end-to-end integration test"
```

---

## Task 11: Vignette + pkgdown + NEWS

**Files:**
- Create: `vignettes/dplyr.Rmd`
- Modify: `_pkgdown.yml`
- Modify: `NEWS.md`

- [ ] **Step 1: Write the vignette**

Create `vignettes/dplyr.Rmd`:

````r
---
title: "dplyr backend"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{dplyr backend}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>")
library(dafr)
```

dafr exposes a dplyr backend for the **axes** of a daf. Each axis
becomes a `tbl` whose rows are the axis entries and whose columns are
the vectors defined on that axis. Matrices are intentionally *not*
exposed through this interface — use the native query DSL for those.

```{r, eval = requireNamespace("dplyr", quietly = TRUE)}
library(dplyr)
d <- example_cells_daf()
tbl(d, "cell")
```

## Familiar verbs

```{r, eval = requireNamespace("dplyr", quietly = TRUE)}
tbl(d, "cell") |>
    filter(!is.doublet) |>
    mutate(log_umis = log10(n_umis)) |>
    group_by(donor) |>
    summarise(n = n(), mean_log = mean(log_umis)) |>
    arrange(desc(n))
```

## Write-back is explicit

`mutate()` stores computed columns in memory. Persisting them as
vectors on the axis requires an explicit `compute(vectors = ...)` —
dafr never silently writes to the daf:

```{r, eval = FALSE}
tbl(d, "cell") |>
    mutate(log_umis = log10(n_umis)) |>
    compute(vectors = "log_umis")
```

`compute()` errors if the tbl has been filtered (partial row mask) —
you cannot write back a partial vector.

## Grouping that looks up an axis

If the grouping variable names an existing axis in the daf, the
result of `summarise()` is a `daf_axis_tbl` keyed to that axis. You
can keep piping dplyr verbs, and later `compute()` the derived
columns back as vectors on that axis.

## Out of scope for v1

- Matrices. Use `Axis(...) |> LookupMatrix(...)` from the query DSL.
- Joins across axes.
- Window functions (`lag`, `cumsum`, etc.).
- DSL pushdown for filter / summarise (planned follow-up).
````

- [ ] **Step 2: Add pkgdown section**

Edit `_pkgdown.yml`. After the "DataFrames" section, add:

```yaml
  - title: dplyr backend
    contents:
      - compute
```

(The `tbl`/`filter`/`mutate`/etc. S3 methods aren't exported symbols so they don't appear in pkgdown, but the vignette will be indexed separately.)

- [ ] **Step 3: Update NEWS**

Edit `NEWS.md`, add an entry at the top (under a new `# dafr (development version)` section if one doesn't exist):

```
* New dplyr backend: `tbl(daf, axis)` returns a lazy `daf_axis_tbl`
  supporting `filter`, `select`, `mutate`, `arrange`, `summarise`,
  `group_by`, `distinct`, `pull`, `collect`. Grouped `summarise()`
  whose grouping variable names an existing axis auto-ties-back to a
  `daf_axis_tbl` on that axis. Write-back to the daf is explicit via
  `compute(tbl, vectors = c(...))`.
```

- [ ] **Step 4: Verify build**

```bash
Rscript -e 'devtools::document()' && R CMD build . 2>&1 | tail -20
```
Expected: build succeeds; vignette renders.

- [ ] **Step 5: Commit**

```bash
git add vignettes/dplyr.Rmd _pkgdown.yml NEWS.md NAMESPACE
git commit -m "docs(dplyr): vignette, pkgdown entry, NEWS"
```

---

## Task 12: Final verification

- [ ] **Step 1: Full test run**

```bash
Rscript -e 'devtools::document()' && R CMD INSTALL . > /dev/null && cd tests && Rscript testthat.R
```
Expected: all dplyr tests pass; pre-existing failures (e.g., `test-helpers.R:26` under Rscript) remain unchanged.

- [ ] **Step 2: Install without dplyr (optional spot check)**

Confirm the package still loads when dplyr is absent:
```bash
Rscript -e 'suppressWarnings(library(dafr)); message("ok")'
```
Expected: no errors, no mention of dplyr (Suggests means it's optional).

- [ ] **Step 3: Manual smoke test**

```bash
Rscript -e '
library(dafr); library(dplyr); library(tibble)
d <- example_cells_daf()
tbl(d, "cell") |>
  filter(n_umis > 1000) |>
  group_by(donor) |>
  summarise(n = n(), mean_umis = mean(n_umis)) |>
  arrange(desc(n)) |>
  print()
'
```
Expected: non-empty tibble printed without error.

- [ ] **Step 4: No further commit needed unless something fails. If something fails, fix forward and commit a new task.**

---

## Follow-ups (explicitly out of scope for this plan)

1. **DSL pushdown for filter.** Detect `col OP const` and `col %in% set` predicates on a single stored vector; translate to `BeginMask(col) |> IsGreater(const) |> EndMask()` etc., avoiding materialization. Real win for FilesDaf / mmap.
2. **DSL pushdown for summarise.** When the expression is one of `{mean,sum,min,max,median}(col)` with no grouping, translate to `Axis(axis) |> LookupVector(col) |> Mean()` etc.
3. **Joins.** Cross-axis joins via matrices (e.g., "attach per-cell mean expression of gene set").
4. **Window functions.** `lag`, `lead`, `cumsum`.
5. **Matrix long-form.** `as_long(daf, rows = "cell", cols = "gene", value = "UMIs")` as an escape hatch.
6. **rowwise() / across().** If users ask for it.

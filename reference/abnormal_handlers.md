# Handler types for abnormal operations

These constants define the different types of handlers for abnormal (but
recoverable) operations.

## Usage

``` r
IGNORE_HANDLER

WARN_HANDLER

ERROR_HANDLER
```

## Format

An object of class `character` of length 1.

An object of class `character` of length 1.

An object of class `character` of length 1.

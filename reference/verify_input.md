# Verify DAF input against contract

Convenience wrapper for verify_contract for input validation.

## Usage

``` r
verify_input(daf_obj, contract)
```

## Arguments

- daf_obj:

  DAF object to validate

- contract:

  Contract to validate against

## Value

Invisible TRUE if valid, throws error otherwise
